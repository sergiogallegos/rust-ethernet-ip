# C# Web HMI Live Validation — 1756-L75/B Firmware 33.011

Date: 2026-08-23

Library: `rust-ethernet-ip` 1.2.1, C ABI 3

Application: [`examples/CSharpWebHmi`](../../examples/CSharpWebHmi/README.md)

## Target

- Processor: ControlLogix `1756-L75/B`, firmware `33.011`
- Communication path: `1756-EN2T` endpoint at `10.136.15.20:44818`
- Route: backplane processor slot `0`
- Mode: strict live PLC (`HMI_FALLBACK_TO_SIMULATION=false`)
- Writes: disabled (`HMI_ALLOW_WRITES=false`)

## Result

`confirmed`: The production frontend bundle and Release ASP.NET backend loaded
the 1.2.1 native library and returned a healthy live snapshot.

| Domain | Reads | Result |
|---|---:|---|
| Controller arrays (`DINT`, `REAL`, `BOOL`, `INT`) | 29 | 29 good |
| Controller `STRING` | 1 | 1 good |
| `TEST_UDT` members | 5 | 5 good |
| `TestProgram` program-scoped tags | 4 | 4 good |
| **Total** | **39** | **39 good, 0 bad** |

The observed snapshot reported:

- mode `Live PLC` and connection state `Connected`;
- native library `1.2.1` and ABI `3`;
- routed target slot `0`;
- 136.35 ms for the first explicit 39-signal scan and 111.2 ms on the
  browser-rendered follow-up scan;
- no fallback or degraded-quality state.

The read-only safety boundary was also exercised. Posting to
`/api/dashboard/pulse` while writes were disabled returned HTTP `400` with the
expected rejection message; no PLC write was attempted.

## Offline Checks

- React/TypeScript production build: pass
- ASP.NET/C# Release build: pass, zero warnings
- Release-readiness source check for 1.2.1: pass
- Release-readiness regression suite: pass
- 1920×1080 simulation and live-mode render inspection: pass
- Forbidden company-name scan within the example: no matches

## Scope

This validates the public example's integration path and selected tag contract.
It does not replace the 2,304-path four-binding release gate or claim that the
example is a production or safety HMI. Writes remained disabled throughout the
live validation.

## Sources

- [`examples/CSharpWebHmi/README.md`](../../examples/CSharpWebHmi/README.md)
- [`examples/CSharpWebHmi/PLC_TAGS.md`](../../examples/CSharpWebHmi/PLC_TAGS.md)
- [`examples/CSharpWebHmi/Services/PlcDashboardService.cs`](../../examples/CSharpWebHmi/Services/PlcDashboardService.cs)
- [`examples/full_coverage_tags.json`](../../examples/full_coverage_tags.json)
