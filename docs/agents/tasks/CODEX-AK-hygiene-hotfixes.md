---
id: CODEX-AK
title: Hygiene hotfixes — semver baseline, dependency scoping, release publish error handling, dead test files, doc corrections
owner: codex
status: open
created: 2026-07-01
last-update: 2026-07-01 claude [Fable 5]
---

## Brief

### Goal

Land the small, independent, high-confidence fixes from the 2026-07-01 repository analysis ([`docs/agents/repo-analysis-2026-07-01.md`](../repo-analysis-2026-07-01.md), §6). Each is mechanical; they bundle because none warrants its own review cycle.

1. **semver gate is toothless** — `.github/workflows/ci.yml` runs `cargo semver-checks check-release --baseline-version 0.7.0`. A 0.x→1.x baseline permits any breaking change, so the gate catches nothing on the 1.x line. Re-point to `1.1.0`.
2. **`vergen` is a runtime dependency** — root `Cargo.toml` lists `vergen = "8.3"` under `[dependencies]` as well as `[build-dependencies]`; nothing under `src/` uses it at runtime (grep-verified). Every crates.io consumer compiles it for nothing. Remove from `[dependencies]`.
3. **`libc` unconditional** — only used in `src/ffi.rs` behind the `ffi` feature. Make it `optional = true` with `ffi = ["dep:libc"]`.
4. **Placeholder author email** — `authors = ["Sergio Gallegos <sergio@example.com>"]` is published crates.io metadata. Confirm the intended value with the maintainer (or drop the email, matching the sibling crates, which omit `authors` entirely — pick one convention for all five manifests).
5. **Release publish swallows all errors** — `release.yml`'s crates.io loop does `cargo publish -p "$crate" || echo "...continuing"`, tolerating auth/network/validation failures, not just already-published. Discriminate: continue only when the error output matches the "already exists" case; fail the job otherwise.
6. **No scheduled cargo-audit** — the audit job runs only on push/PR, so new RUSTSEC advisories against an idle `main` go unnoticed. Add a weekly `schedule:` trigger.
7. **Dead test files** — `tests/unit/element_addressing_tests.rs` is never compiled (cargo only builds top-level `tests/*.rs`; nothing `mod`-includes `tests/unit/`) and has diverged from its top-level twin. Delete the `tests/unit/` directory. Also delete `test_isolated/` at the repo root (single scratch `test_connection.cs` + csproj, referenced by nothing).
8. **`tests/release_readiness_tests.sh` not in CI** — the other three shell suites run in CI; wire this one into the same job.
9. **README overstates NuGet RIDs** — README claims the package bundles `osx-x64`; `release.yml` intentionally omits it (3 RIDs ship: win-x64, linux-x64, osx-arm64). Correct the claim (macOS-Intel restoration is ROADMAP item 3).
10. **CLAUDE.md FFI build command** — the build section says `cargo build --release` is what's needed for the C# FFI cdylib; without `--features ffi` that produces a cdylib with no `eip_*` exports (the exact cause of a past CI EntryPointNotFound incident). Fix the command and the sentence. (Full CLAUDE.md architecture refresh is ROADMAP item 1 — do *not* attempt it here.)
11. **NuGet csproj native-lib layout** — `csharp/RustEtherNetIp/RustEtherNetIp.csproj` includes the native library via OS-conditional `Content` items from `../../target/release/`, so a locally produced package lacks `runtimes/{rid}/native/` layout. Restructure to stage into `runtimes/{rid}/native/` so a local `dotnet pack` produces a package with correct (single-RID) native resolution. The multi-RID release pipeline must keep working unchanged.

### Context to read first

- `docs/agents/repo-analysis-2026-07-01.md` §6.
- `.github/workflows/ci.yml` (semver job, audit job, shell-test job) and `.github/workflows/release.yml` (crates-io publish loop, nuget packaging steps).
- Root `Cargo.toml` + the four `crates/*/Cargo.toml` (authors convention).
- `scripts/check-release-readiness` + `check-release-readiness.txt` — item 4 and 11 may touch readiness-checked locations; keep the manifest in sync.

### Files to create or modify

`.github/workflows/ci.yml`, `.github/workflows/release.yml`, `Cargo.toml` (+ sibling manifests for the authors decision), `Cargo.lock` (regenerated by the dependency edits only — no bulk update), `csharp/RustEtherNetIp/RustEtherNetIp.csproj`, `README.md`, `CLAUDE.md` (build section lines only), delete `tests/unit/` and `test_isolated/`, `rust-ethernet-ip.sln` (if it references test_isolated — verify), `CHANGELOG.md`.

### Behavior

No library behavior changes. `cargo build` (default features) no longer compiles `vergen` or `libc`. CI fails on a genuine semver break against 1.1.0 and on a genuine publish failure.

### Test requirements

- `cargo build --locked` and `cargo build --features ffi --locked` both succeed after the manifest edits.
- `cargo semver-checks check-release --baseline-version 1.1.0` passes locally against current `main` (it should — everything since 1.1.0 is additive; if it fails, stop and report, don't suppress).
- `dotnet pack csharp/RustEtherNetIp` locally and inspect the nupkg layout (`unzip -l`): native lib under `runtimes/<rid>/native/`.
- `scripts/check-release-readiness 1.1.0 --skip-package` still passes.
- Standard gates: fmt, clippy `-D warnings`, `SKIP_PLC_TESTS=1 cargo test --workspace --locked`, `cargo test --test plc_sim_tests`, C# `dotnet test`.

### Acceptance criteria

- All 11 items done or explicitly reported as blocked (item 4 needs a maintainer answer — proceed with the "omit email" convention if no answer, note it in the log).
- grep proves: no `vergen` under `[dependencies]`; `libc` optional; no `tests/unit/`; no `test_isolated/`.
- CI YAML parses; the semver job's baseline is `1.1.0`; audit has a `schedule:`; release_readiness_tests.sh runs in CI.
- CHANGELOG `[Unreleased]` updated (the dependency-scoping items are consumer-visible and belong there; also fold in the pre-existing unlogged `futures` default-features change from `47b99a4` if the maintainer agrees — flag it in the log).

### Out of scope

- The full documentation refresh (ROADMAP item 1) — only the two surgical doc corrections above.
- eframe/egui upgrade for the desktop example (drives the audit.toml ignores; tracked separately).
- Python ≥3.13 CI matrix additions.
- cargo-deny (ROADMAP item 12).

### Risks and gotchas

- **Never bulk-run `cargo update`** (CLAUDE.md rule). Removing a dependency edits the lockfile minimally; verify the diff touches only the removed/optional entries.
- semver-checks compares against the *published* 1.1.0 on crates.io — needs network in CI; the job already handles that for 0.7.0, so only the version string changes.
- The csproj change interacts with the release workflow's multi-RID staging — read how `release.yml` assembles the package before touching `Content` items; the release path must remain the source of truth for published packages.
- `check-release-readiness.txt` pins strings in some of the files touched here (README, csproj); run the readiness script before submitting to catch accidental parity breaks.

## Codex log

## Claude review

## Verdict
