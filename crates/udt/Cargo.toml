[package]
name = "rust-ethernet-ip-udt"
version = "1.0.0"
edition = "2024"
license = "MIT"
description = "Logix UDT parsing helpers used by rust-ethernet-ip"
rust-version = "1.95"

[lib]
name = "rust_ethernet_ip_udt"
path = "src/lib.rs"

[dependencies]
rust-ethernet-ip-types = { path = "../types", version = "1.0.0" }

[dev-dependencies]
tracing = "0.1"
