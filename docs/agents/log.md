# Agent Activity Log

Append-only chronological transcript of cross-agent activity. One line per event. Newest at bottom.

Format: `YYYY-MM-DD HH:MM  <author>  [<model>]  <task-id-or-->  <event>`

Use `--` for task-id when the event is project-wide (protocol bootstrap, etc). The `[<model>]` tag identifies the underlying model that produced the entry — e.g. `[Opus 4.7]`, `[Sonnet 4.6]`, `[gpt-5.5]`. Convention introduced 2026-05-17; entries above this note predate the convention and stay as-is (the log is append-only).

---

2026-05-01  claude  --        Cross-agent collaboration protocol bootstrapped at docs/agents/. Templated from OpenWebHMI HEAD; rust-ethernet-ip CLAUDE.md gained an "Agent collaboration" appendix pointing at this directory.
2026-05-05  claude  CODEX-A   Brief authored: FFI safety, runtime hardening, and lint baseline. Gates `pub mod ffi;` on the existing `ffi` Cargo feature, surfaces `Runtime::new()` failure via FFI return code, converts `RwLock`/`HashMap` unwraps at FFI-reachable sites, and adds a conservative crate-level `#![deny]` / `#![warn]` baseline. Status: open.
2026-05-05  codex   CODEX-A   Implementation submitted: gated FFI behind `--features ffi`, added fallible runtime initialization return code, converted FFI-reachable poison/unwrap paths, added lint baseline, and verified Rust/C# build and tests. Status: submitted.
2026-05-05  claude  CODEX-A   Merged at `3d98abf`. Independent verification: cargo fmt clean, both clippy feature variants clean, full test matrix green, symbol parity verified (56 eip_ exports under --features ffi, 0 in default rlib). Four polish notes documented as non-blocking candidates for CODEX-B. Status: merged.
2026-05-05  claude  CODEX-B   Brief authored: contained API cleanup. BatchError → thiserror, drop async-trait direct dep, fix 0.7.0 → 0.8.0 docstring rot, delete tag_subscription.rs shim, remove three unused EipClient fields, add #[must_use] to selected builders/getters, plus two cheapest polish items carried over from CODEX-A (RAII guard for FORCE_RUNTIME_INIT_ERROR, ffi_block_on! contract doc). Excludes any SemVer-major change. Status: open.
2026-05-05  codex   CODEX-B   Implementation started. Status: in-progress.
2026-05-05  codex   CODEX-B   Implementation submitted: converted BatchError to thiserror, removed unused async-trait edge and tag_subscription shim, removed unused EipClient fields, added selected must_use annotations, and applied CODEX-A test/macro polish. Rust and C# verification passed. Status: submitted.
2026-05-05  claude  CODEX-B   Merged at `9aca8d2`. Independent verification: cargo fmt clean, both clippy feature variants clean, full test matrix green, BatchError Display strings byte-identical to prior manual impl, FFI symbol parity preserved at 56 eip_ exports, async-trait absent from active dep graph. Two non-blocking polish notes recorded; one brief-text error owned by Claude (audit-grep pattern). Status: merged.
2026-05-05  claude  CODEX-C   Brief authored: pure-mechanical decomposition of the 8.3k-line lib.rs into route.rs, batch.rs, types.rs, and client.rs across four phases. No behavior change, no signature change. Gating step for CODEX-D. Status: open.
2026-05-05  claude  CODEX-D   Brief authored: extract pub(crate) Encode/Decode traits for the wire protocol; move PlcValue, EncapsulationHeader, and CIP framing into src/protocol/. Round-trip codec tests required (≥30); benchmarks must show no >5% regression. Depends on CODEX-C. Status: open.
2026-05-05  claude  CODEX-E   Brief authored: four small polish items — Once-based runtime-init log dedupe, merge adjacent pub use subscription blocks, LazyLock the tag-name regex, drop the unused cargo-tarpaulin dev-dep. Independent of CODEX-C/D. Status: open.
2026-05-05  codex   CODEX-E   Implementation started. Status: in-progress.
2026-05-05  codex   CODEX-E   Implementation submitted: runtime-init logs deduped with Once, subscription re-exports merged, tag-name regex cached with LazyLock, cargo-tarpaulin dev-dependency removed, and Rust/C# verification passed. Status: submitted.
2026-05-05  claude  CODEX-E   Merged at `fc63735`. Independent verification: cargo fmt clean, both clippy feature variants clean, full test matrix green, FFI symbol parity preserved at 56 eip_ exports. Two non-blocking polish notes recorded; one brief-text error owned by Claude (Cargo.lock is gitignored and never committed in this repo, so the brief's lockfile-shrinkage instruction was wrong; future briefs must run `git ls-files Cargo.lock` before assuming tracked-lockfile semantics). Status: merged.
2026-05-05  codex   CODEX-C   Implementation started. Status: in-progress.
2026-05-05  codex   CODEX-C   Implementation submitted: decomposed lib.rs into route.rs, batch.rs, types.rs, and client.rs; lib.rs reduced to 221 lines; CHANGELOG updated; Rust/C# verification passed and FFI symbol parity remained 56 eip_ exports. Status: submitted.
2026-05-05  claude  CODEX-C   Merged at `476f21c`. Independent verification: cargo fmt clean, both clippy feature variants clean, full test matrix green, cargo doc clean (zero broken intra-doc link warnings), FFI symbol parity preserved at 56 eip_ exports. Public API parity verified by reading the full re-export block. Two non-blocking polish notes recorded; no brief errors owned by Claude this round. CODEX-D now unblocked. Status: merged.
2026-05-05  codex   CODEX-D   Implementation started after CODEX-C merge. Status: in-progress.
2026-05-05  codex   CODEX-D   Question raised on benchmark stop condition: post-change `cargo bench` reported >5% regressions (e.g. single_tag_read 1.1106 ns → 1.4377 ns; batch_read/tags/5 54.127 ns → 117.95 ns). Implementation paused per brief; awaiting maintainer/Claude direction. Status: in-progress.
2026-05-08  claude  CODEX-D   Answered: bench gate overruled because benches/performance_benchmark.rs is a placeholder file that measures enum construction and vec allocation, not the codec path. Brief error owned by Claude — the >5% read/write regression gate assumed real benchmarks existed. Codex directed to proceed with submission; functional verification (32 codec round-trip tests, plc_sim_tests, dotnet wrapper) is the real correctness gate. Real codec benchmarks logged as future polish item, not bundled into CODEX-D. Status: in-progress.
2026-05-08  codex   CODEX-D   Implementation submitted: added crate-private protocol codec modules, wired EipClient through encapsulation/CIP/value codecs, added 32 codec tests, preserved 56 eip_ FFI exports, and passed Rust/C#/simulator verification. Status: submitted.
2026-05-08  claude  CODEX-D   Merged at `c58a905`. Independent verification: cargo fmt clean, both clippy feature variants clean, full test matrix green (lib tests 56 → 87, +31 codec tests), FFI symbol parity preserved at 56 eip_ exports, cargo doc clean. Pinned-byte fixtures lock down AB STRING padding, AB UDT type-tag synthesis, and CIP error response wire format. Zero inline 13-type encoders remain in client.rs (verified by grep). Four non-blocking polish notes recorded; bench gate overruled per prior answer entry. With CODEX-D merged, all five second-pass review briefs (A-E) are complete. Status: merged.
2026-05-14  codex   CODEX-F   Implementation submitted in response to GitHub issue on ordered CIP routing — without a prior Claude-authored brief. First submission directionally correct but had three blockers: clippy --all-features lint failure, broken connect_with_stream doctest, ethernet hop encoding used raw IPv4 octets instead of the spec-correct ASCII extended link-address shape. Status: submitted.
2026-05-14  codex   CODEX-F   Follow-up fixes addressed all three blockers: replaced manual mod-2 with is_multiple_of, doctest now uses std::io::Cursor instead of TcpStream, ethernet hops emit `[0x10|port, ascii_len+1, ascii…, 0x00, optional_pad]`. Added five pinned-byte tests including the legacy-public-field fallback path. Status: submitted.
2026-05-14  claude  CODEX-F   Brief authored retroactively. Merged at `9a3d192`. Independent verification: cargo fmt clean, both clippy feature variants clean, full test matrix green (udt_discovery_tests 11 → 18 with 5 new pinned-byte tests), FFI symbol parity preserved at 56 eip_ exports. Dual-state hazard from initial submission mitigated via `if hops.is_empty()` fallback; legacy fields preserved for non-breaking 0.8.0 ship. Process note recorded in verdict: this is the first off-protocol submission of the session — Codex acted on the GitHub issue without a brief; review caught two real defects plus an architectural concern; retroactive brief authoring is the agreed cleanup. SemVer-major private-storage RoutePath reshape tracked for the 1.0.0 release-window brief. Hardware validation against a real multi-hop topology still pending; wiki marks ethernet encoding correctness as `likely` rather than `confirmed`. Status: merged.
2026-05-14  --      --        Session paused at maintainer request. All work pushed to origin. Forward agenda durably recorded in `board.md` under "Next agenda": gating items (hardware validation, CHANGELOG roll), 0.9.0 release cut, architecture doc refresh, CODEX-G through CODEX-J non-breaking polish briefs, and CODEX-K release-window brief bundling every deferred SemVer-major item for the 1.0.0 cut. Next session reads `board.md` first per protocol; the "Next agenda" section is self-contained and does not require chat history to act on.
2026-05-17  claude  [Opus 4.7]  --        Convention: agent + model tag added to log entries, task section headers, and frontmatter `last-update` (e.g. `claude [Opus 4.7]`, `codex [gpt-5.5]`). Format documented in `docs/agents/README.md` and `CLAUDE.md`; entries before this line predate the convention.
2026-05-17  claude  [Opus 4.7]  --        v0.8.0 state clarified: per maintainer direction, all CODEX-A→F merges belong to the v0.8.0 draft (sits on `main`, no tag, no publish), held pending hardware validation. Board "Project context" corrected (last released = v0.7.0), "Next agenda" reframed (drop v0.9.0 plan, gating items now target v0.8.0 cut). CHANGELOG `[Unreleased]` gained the missing CODEX-F `RouteHop` entry. Architecture doc rot fixed in `CLAUDE.md` and `docs/SOFTWARE_ARCHITECTURE.md` (lib.rs description, module table, design-debt list).
2026-05-18  claude  [Opus 4.7]  --        Post-books architecture review committed at `wiki/investigations/architecture-review-2026-05-18.md`. Synthesises five reference texts (Clean Architecture, DDIA, Understanding Distributed Systems 2e, PEAA, Rust for Network Programming and Automation) against the current codebase, reconciled with an independent codex second pass. Three reviewer disagreements resolved on the record: public Transport trait recommendation withdrawn (EtherNetIpStream at lib.rs:69-102 already covers the seam), actor refactor reclassified as behaviorally breaking (not internal), FFI ABI versioning promoted to Phase 0 (was Phase 2). Three new findings from the codex pass added to the roadmap: FFI registry clone semantics (CODEX-M), CIP path encoding silent truncation (CODEX-N supersedes the debug_assert mention in CODEX-H), `PlcValue::Udt::get_data_type` placeholder (CODEX-O).
2026-05-18  claude  [Opus 4.7]  CODEX-L   Brief authored: FFI ABI version + capability handshake. Adds `eip_abi_version()`, `eip_library_version()`, `eip_capabilities()` exports; pins current FFI shape as ABI v1; wires C# `NativeRuntime` + Python `bindings.ABI_VERSION` load-time mismatch checks. Phase 0 item 1; must run before CODEX-M and CODEX-N. Queued for post-0.8.0 activation. Status: open.
2026-05-18  claude  [Opus 4.7]  CODEX-M   Brief authored: FFI registry clone-semantics audit and fix. Two-phase brief — Phase A audit findings in Codex log, Phase B implementation after Claude reviews the option choice (A: drop Clone; B: introduce `ClientHandle`; C: move every mutated scalar into Arc/Mutex). Establishes that no FFI function silently mutates a field other FFI functions cannot observe. Stress test: 1,000 alternating mutate/observe iterations via simulator. Phase 0 item 2; runs after CODEX-L. Status: open.
2026-05-18  claude  [Opus 4.7]  CODEX-N   Brief authored: CIP path encoding hard validation. Replaces silent truncation/overflow in `CipRequest::encode` (`src/protocol/cip.rs:29,35`) with `Result<Bytes, EtherNetIpError>` return + parity / overflow / empty-path checks. Adds property + negative tests in `protocol/tests.rs`. Supersedes the `debug_assert!` mention in CODEX-H — removed from CODEX-H agenda entry in the same commit. Phase 0 item 3. Status: open.
2026-05-18  claude  [Opus 4.7]  --        Books lessons durably captured at `wiki/investigations/books-lessons-2026-05-18.md` — per-book extraction with file:line citations, mapping each lesson to the CODEX brief that tracks it (or noting "no brief, deferred / no brief, bundle into existing"). Lists deliberately-not-applied concepts (CQRS, Saga, generic message bus, tag-path DSL) so future sessions don't re-debate them.
2026-05-18  claude  [Opus 4.7]  CODEX-V   Brief authored: Add cargo-semver-checks to CI as the SemVer gate. Defensive infrastructure for the v0.8.0 cut given that CODEX-K release-window + CODEX-P actor + CODEX-N new error variant + #[non_exhaustive] sweep all land in the same version. PR mode is `continue-on-error: true`; main-branch mode is required. Status: open.
2026-05-18  claude  [Opus 4.7]  --        Scope decision per maintainer directive: every brief in this plan (CODEX-L through CODEX-V) ships in v0.8.0 alongside the pre-existing CODEX-G through CODEX-K agenda. Activation-gate language in `board.md` lifted. Architecture review updated with sequencing inside v0.8.0 and the honest note that the resulting bundle is 1.0.0-shape (FFI contract pin + behavioral refactor + new public API + structural split + release-window break sweep) — version-naming left to maintainer.
2026-05-24  claude  [Opus 4.7]  --        Hardware validation run against maintainer's ControlLogix 1756-L75 fw33 + 1756-EN2T topology (10.136.15.20). Rust core: PASS — `cargo fmt`, `cargo clippy`, `SKIP_PLC_TESTS=1 cargo test --workspace --locked` (208/208), `cargo test --test plc_sim_tests` (10/10), full integration suite vs PLC (208 passed, 0 failed, 56 ignored on `#[ignore]`-gated topologies), `test_plc_test_tag_definitions` (333/333 writeable tags verified + restored; 59 firmware-blocked writes correctly rejected client-side). Random→verify→nines exerciser (27 tags spanning all data types + both scopes): Rust 27/27/27 PASS, C# wrapper 27/27/27 PASS, Python wrapper 23/27 FAIL with 4 plain BOOL-array writes returning CIP 0x1E. Single-tag bench (500 iters of `gTestArray_DINT[0]`): Rust 174.5 read ops/sec / 188.0 write ops/sec, C# 130.8 / 156.4, Python 162.7 / 325.9. C# batch_read measured at 1971 logical ops/sec. Direct-connect topology validates the CODEX-F backplane-hop path; multi-chassis ethernet hop encoding still pending a 2-chassis bench.
2026-05-24  claude  [Opus 4.7]  CODEX-W   Brief authored: Python wrapper — route single-tag writes through typed FFI exports. Root cause for the BOOL-array regression found during the 2026-05-24 hardware run — `python/rust_ethernet_ip/bindings.py` registers zero typed single-tag write FFI exports, so every `Client.write_tag` is funneled through `eip_execute_batch`, which the PLC rejects with CIP 0x1E for plain BOOL[] element writes (UDT-nested BOOLs hit a different CIP path and work). Brief wires `eip_write_bool / sint / int / dint / lint / usint / uint / udint / ulint / real / lreal / string` into `bindings.py`, replaces `Client.write_tag` body with typed dispatch keyed on existing `_infer_value_type`, keeps `Client.write_tags` (multi-item) on the batch path, keeps `read_tag` JSON-based, leaves `eip_write_udt` for a separate brief. Tests: simulator integration (BOOL[] write/read, all-typed roundtrip, out-of-range ValueError); unit (mocked native lib confirming typed-export dispatch); hardware re-run output to be recorded in `## Codex log`. Parallel-safe with CODEX-L (the other open bindings.py-toucher). Status: open.
2026-05-24  codex   [gpt-5]     CODEX-L   Submitted: FFI ABI handshake implemented with `eip_abi_version`, `eip_library_version`, and `eip_capabilities`; C# and Python wrappers now verify ABI v1 at load time and expose version/capability metadata. Added `wiki/protocol/abi-contract.md`. Verification passed: Python ABI/import/bindings unit tests, `cargo test --features ffi --test ffi_abi`, and C# wrapper tests. Status: submitted.
2026-05-24  codex   [gpt-5]     CODEX-N   Submitted: `CipRequest` now validates empty, odd-length, and over-510-byte paths before encoding; request builders use the checked encoder; deterministic protocol tests cover valid path lengths through 510 bytes and the negative cases. Added `wiki/protocol/cip-path-validation.md`. Verification passed: `cargo test protocol::tests::cip --locked`. Status: submitted.
2026-05-24  codex   [gpt-5]     CODEX-V   Submitted: CI now includes a `cargo-semver-checks` job using `taiki-e/install-action@v2`, stable Rust, the crates.io `0.7.0` baseline, PR-only `continue-on-error`, and final build gating via `needs: semver-checks`. Not run locally because the sandbox has restricted network access. Status: submitted.
2026-05-24  codex   [gpt-5]     CODEX-M   Phase A audit completed and recorded in the task file. Current implemented FFI mutation paths either update the registry entry directly, store the clone back, or mutate Arc-backed fields; copied scalar risks are `session_handle`, `route_path`, `max_packet_size`, and `batch_config`. Recommended Option C and paused Phase B pending Claude review as required by the brief. Added `wiki/wrapper-parity/ffi-registry-clone-audit.md`. Status: under-review.
2026-05-24  codex   [gpt-5]     --        Final local verification for CODEX-L/M/N/V/X/Y pass: `cargo fmt -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace --all-features --locked`, `cargo test --test plc_sim_tests --locked`, `PYTHONPATH=python python3 -m unittest discover -s python/tests`, `dotnet test csharp/RustEtherNetIp.Tests/RustEtherNetIp.Tests.csproj --no-restore -v minimal`, and `git diff --check`. `cargo-semver-checks` was not run locally because it requires network/tool installation in this sandbox.
2026-05-24  codex   [gpt-5]      CODEX-W   Implementation started: registering Python typed read/write FFI signatures, replacing single-tag scalar write dispatch with typed exports, adding BOOL-array/all-numeric simulator coverage, and preserving `write_tags()` plus UDT single-write fallback on the batch path. Status: in-progress.
2026-05-24  codex   [gpt-5]      CODEX-W   Implementation submitted: Python `bindings.py` now registers typed scalar read/write FFI exports; `Client.write_tag()` dispatches scalar and STRING writes through typed exports with range validation and leaves UDT on batch fallback; simulator gained BOOL array plus SINT/INT/LINT/USINT/UINT/UDINT/ULINT/LREAL tags; Python unit/integration coverage added for typed dispatch, BOOL-array write/read, numeric roundtrips, and pre-FFI range errors. Verification passed: focused Python units, simulator-backed Python integration, full Python unittest discovery, `cargo build --example python_test_simulator`, `cargo test --test plc_sim_tests`, `SKIP_PLC_TESTS=1 cargo test --workspace --locked`, `cargo clippy -- -D warnings`, `rustfmt --edition 2024 --check tests/plc_sim.rs`, and `git diff --check`. Full `cargo fmt --check` remains blocked by unrelated active hardware-validation examples. Status: submitted.
2026-05-24  claude  [Opus 4.7]  --        Full-coverage hardware exerciser run against maintainer's ControlLogix 1756-L75 fw33 + 1756-EN2T (10.136.15.20). 2299 distinct tag targets per binding (controller + program scope; every element of DINT[100], REAL[50], BOOL[128], INT[200], Large[1000], plus all UDT members and gTestUDT_Array[0..9] member sweep). Rust: 2109/2299 reads OK, 1806/1806 writes OK, 78 BOOL verify-mismatches; C#: 2099/2299 reads, 2006/2206 writes (200 nested-BOOL writes fail), 91 BOOL verify-mismatches; Python (with CODEX-W fix applied): 2109/2299 reads, 2006/2206 writes, 76 BOOL verify-mismatches. **Two new Rust-core findings** affecting all three bindings equally — surfaced for the first time at this coverage depth: (1) plain `gTestArray_BOOL[i]` writes return Ok but readback shows ~30-40% mismatch (RMW / bit-write path bug), (2) `gTestUDT_Array[i].Array_BOOL[j]` reads and writes both fail wholesale (200/200 across both directions). Worth dedicated CODEX-X / CODEX-Y briefs before tagging v0.8.0. Examples used: `examples/test_plc_full_coverage.rs`, `examples/CSharpFullCoverage/`, `python/examples/test_plc_full_coverage.py` (all uncommitted scratch this session; commit decision pending).
2026-05-24  claude  [Opus 4.7]  CODEX-W   Reviewed: independent verification reproduced every check Codex claimed (fmt/clippy/workspace-test/sim-test/Python-unit/Python-integration). Hardware re-run against the maintainer's PLC via `python/examples/test_plc_full_coverage.py` confirmed zero CIP 0x1E errors on plain BOOL-array writes — 128/128 ctrl + 100/100 program, matching Rust/C# behavior exactly. Implementation is clean: typed FFI dispatch with correct argtypes (notably `c_double` for REAL/LREAL matching the Rust `f64` FFI signature and C# wrapper convention; `c_int` for `eip_write_bool` per the gotcha), BOOL-before-int ordering for Python's bool-as-int quirk, integer range validation before crossing FFI boundary, UDT fallback retained. Tests are thorough: parametrized 11-export dispatch test, STRING-specific test, UDT-stays-on-batch pin, pre-FFI ValueError test, plus simulator round-trip integration. Two non-blocking polish notes (redundant .upper() after _infer_value_type, int-accepted-for-REAL by design). Zero real concerns. Status: merged.
2026-05-24  claude  [Opus 4.7]  --        Diagnostic exercisers committed at `3d5592a`: cross-binding random→verify→nines (27 tags) and full-coverage (2299 tags) exercisers in Rust, C#, and Python, plus single-tag read/write benchmarks in all three languages. Honour `TEST_PLC_ADDRESS` / `TEST_PLC_SLOT` and target the spec in `docs/PLC_TEST_TAG_DEFINITIONS.md`. Also adds `VISIBLE_PAUSE_SECS` env var to `examples/test_plc_test_tag_definitions.rs` so operators can observe writes in Studio 5000 between verify and restore. Re-runnable against any future PLC topology.
2026-05-24  claude  [Opus 4.7]  CODEX-X   Brief authored: BOOL array element RMW addresses the wrong DWORD for indices ≥ 32. Root cause located in `read_bool_array_element_workaround` (`src/client.rs:1052`) and `write_bool_array_element_workaround` (`src/client.rs:1529`): both hardcode `build_read_request_with_count(base_array_name, 1)`, which resolves to DWORD[0] only. Every BOOL access at index ≥ 32 reads/writes the wrong DWORD. Diagnosis verified by aliasing math against the 2026-05-24 hardware verify-rate evidence (62.5% expected, 58-70% observed across Rust/C#/Python). Fix: route through `build_read_array_request(base, index / 32, 1)` + mirror on the write side. Tests: simulator BOOL_ARRAY expanded to ≥64 elements for cross-DWORD coverage, pinned-byte test for the element-segment encoding, hardware re-run on full-coverage exerciser expects 128/128 ctrl + 100/100 program BOOL verify. v0.8.0 gating. Parallel-safe with CODEX-Y. Status: open.
2026-05-24  claude  [Opus 4.7]  CODEX-Y   Brief authored: BOOL workaround not applied to nested BOOL arrays inside UDT array elements. Symptoms captured live on hardware: `gTestUDT_Array[3].Array_BOOL[0]` returns `Udint(0)` (whole DWORD, not a Bool); other indices return CIP 0x05 "Path destination unknown". Root cause: `read_tag` dispatch (`src/client.rs:854-902`) routes complex paths through `build_read_request` without BOOL detection — only simple top-level array paths reach `read_bool_array_element_workaround`. Fix: add complex-path BOOL detection via `TagPath::parse` → `TagPath::Array { base_path, index }`, probe parent path for data type 0x00D3, apply equivalent RMW bit-extraction on the parent path. Same change on the write dispatch. Tests: simulator UDT with BOOL[] member, regression test pinning Bool-not-Udint return for index 0. Hardware re-run expects 200/200 nested BOOL reads + writes (currently 0/200 + 0/200 respectively when correctly classified). v0.8.0 gating. Parallel-safe with CODEX-X (the DWORD-offset arithmetic is identical between the two — landing order doesn't matter, second-to-land trivially rebases). Status: open.
2026-05-24  codex   [gpt-5]      CODEX-X   Implementation submitted: BOOL array RMW reads/writes now address `index / 32` via element-addressed DWORD requests and keep bit math at `index % 32`; simulator BOOL array expanded to 64 packed bits; cross-DWORD read/write and pinned DWORD[1] element-segment tests added. Status: submitted.
2026-05-24  codex   [gpt-5]      CODEX-Y   Implementation submitted: complex final array paths now probe the parent path for packed BOOL-array DWORD data and route nested BOOL array reads/writes through the shared RMW helper; simulator path parsing now retains non-final array indices for nested keys; nested BOOL array regression tests added. Status: submitted.
2026-05-24  claude  [Opus 4.7]  --        Baseline verification reproduced for the 5-brief bundle: cargo fmt clean, cargo clippy --workspace --all-targets --all-features -D warnings clean, SKIP_PLC_TESTS=1 cargo test --workspace --all-features --locked → 218 passed (was 208), cargo test --test plc_sim_tests → 13/13 (was 10/10, +3 from CODEX-X and CODEX-Y), Python full discover → 35 passed + 8 skipped, dotnet test → 79/79 after rebuilding the cdylib with --features ffi to pick up the new ABI exports. The 0xC2 missing-symbol failure pre-rebuild confirms the wrappers correctly fail-fast on stale dylibs (CODEX-L behavior validation).
2026-05-24  claude  [Opus 4.7]  CODEX-L   Reviewed: independent verification reproduced every Codex check plus full workspace re-run. Implementation is clean — ABI_VERSION=1 plus four named CAP_* constants in src/version.rs, three new FFI exports use OnceLock for the version string (no per-call alloc, no leak), C# NativeRuntime throws BadImageFormatException on mismatch (semantically correct exception class), Python _verify_abi gates typed-signature configuration on a successful handshake (defensive ordering), __getattr__ lazy export avoids forcing native-lib load at import time. Pinned tests cover the mismatch path on both wrappers. wiki/protocol/abi-contract.md documents bump policy. Two non-blocking polish notes: stale MAJOR/MINOR/PATCH_VERSION constants in src/version.rs predate this brief (CODEX-H followup); FakeNativeLibrary.__getattr__ defaults to FakeFunction(0) for unknown attributes which is a future-footgun. Zero real concerns. Status: merged.
2026-05-24  claude  [Opus 4.7]  CODEX-N   Reviewed: independent verification passes. CipRequest::validate split out as pub(crate) method, encode wraps as self.validate()?, u8::try_from(...).expect(...) pattern correctly documents the upstream invariant. All three failure modes (empty path, odd length, > 510 bytes) get distinct actionable error messages with the offending service code. Negative tests pin both the error message AND buf.is_empty() — the no-partial-write guarantee made explicit. Boundary test covers powers of two through exactly 510 bytes. All CipRequest call sites in client.rs correctly propagate the new Result via ?. CODEX-H agenda updated to drop the now-superseded debug_assert mention. Two non-blocking polish notes (per-failure format! alloc cost; could factor out a service_code-bearing error variant). Zero real concerns. Status: merged.
2026-05-24  claude  [Opus 4.7]  CODEX-V   Reviewed: YAML structure correct, matches existing job-style conventions (taiki-e/install-action + Swatinem/rust-cache like cargo-audit). continue-on-error conditional gives PR-advisory / main-blocking asymmetry per the brief. build job needs: array updated to include semver-checks so artifacts are gated on a green API story. cargo-semver-checks couldn't be executed locally (network-restricted sandbox); first PR run will exercise it. One acceptance criterion deferred: deliberate-API-break test branch not executed (sandbox limit, documented by Codex). One polish: baseline pinned to literal "0.7.0" string instead of --baseline-rev; equivalent semantics, requires one-line bump per release. Zero real concerns. Status: merged. Pre-release reminder: bump baseline pin to 0.8.0 when promoting CHANGELOG Unreleased.
2026-05-24  claude  [Opus 4.7]  CODEX-X   Reviewed: independent verification + hardware re-run against ControlLogix 1756-L75. The DWORD-offset fix at src/client.rs:1128 (read) + :1593-1599 (write) is surgical and exactly what the brief asked for. Codex factored out parse_bool_array_dword_response as a shared helper used by both top-level and CODEX-Y nested paths — net 53 LOC removed despite adding new behavior. Dead code purge: build_write_request_with_data removed (its sole caller now uses build_write_array_request_with_index), build_write_request_raw rewritten to use CipRequest::encode (picks up CODEX-N validation transitively). bool_array_dword_index_uses_element_segment pins the wire-byte encoding (0x28 0x01 element segment). Hardware confirms: ctrl.BOOL_array verify rose from 79/128 → 128/128, prog.BOOL_array from 71/100 → 100/100. The aliasing model's 62.5% theoretical match rate is gone; clean 100% across all 228 plain BOOL elements. Zero real concerns. Status: merged.
2026-05-24  claude  [Opus 4.7]  CODEX-Y   Reviewed: independent verification + hardware re-run. Detection dispatch at src/client.rs:894-902 (read) and :3114-3121 (write) is symmetric — both use parse_final_array_element_access + detect_bool_array_path. The single-index guard (indices.len() == 1) correctly bails on multi-dim arrays. Detection probe uses count=1 test-read to check for BOOL_ARRAY_DWORD data type, defensive against false-positive routing. Same read_bool_array_element_workaround reused for both simple and nested paths — DRY without over-engineering. Simulator extended with UDT_ARRAY[3].BOOL_NESTED member; pinned matches!(_, PlcValue::Bool(_)) test guards against the Bool-not-Udint regression. Hardware confirms: ctrl.UDTarr_elem_nested 350/350 reads (was 160/350), CIP 0x05 errors gone, Bool-not-Udint at index 0 fixed. Combined with CODEX-X: total reads 2299/2299, verify 1806/1806, real anomalies 0 (down from 268). Two non-blocking polish notes (silent Ok(false) on detection-probe CIP error; per-access detection probe round-trip — future caching opportunity). Zero real concerns. Status: merged.
2026-05-24  claude  [Opus 4.7]  CODEX-M   Phase A audit reviewed and confirmed. Spot-checked four copied scalars against src/ffi.rs: route_path uses get_mut not get_client clone (currently safe); max_packet_size FFI is a stub; batch_config has intentional per-call ephemeral mutation; session_handle is set pre-insert. No actual bug today, but structural risk for future contributors. Option choice CONFIRMED as C (not A: too restrictive; not B: disproportionate for 4-field problem, leaves seam for CODEX-P actor refactor). PUSH-BACK on Codex's documentation-only execution: brief is explicit that Option C means actually moving fields to Arc<Mutex>/Arc<Atomic>, not just adding SHARED/COPIED comments. Phase B direction given in task file: route_path → Arc<Mutex<Option<RoutePath>>>, max_packet_size → Arc<AtomicU32> (cheaper than Mutex, preempts stub footgun), session_handle and batch_config stay copied with documented invariants. Plus the compile_fail Send+Sync check and tests/ffi_state_consistency.rs 1000-iter regression test per the brief — NOT deferred. Estimated ~140 LOC. Status: in-progress (Phase B).
2026-05-24  codex   [gpt-5]     CODEX-M   Phase B submitted: `route_path` moved to shared `Arc<Mutex<Option<RoutePath>>>`, `max_packet_size` moved to `Arc<AtomicU32>`, copied `session_handle`/`batch_config` invariants documented, `eip_set_route_path` now safely mutates cloned registry clients, and `eip_set_max_packet_size` now mutates shared state. Added `tests/ffi_state_consistency.rs` with 1,000 FFI mutate/observe iterations and an FFI state-mutation benchmark. Verification passed: Rust fmt/clippy/workspace tests, focused FFI/route tests, Python unittest discovery, C# wrapper tests, and `cargo bench --features ffi --bench performance_benchmark -- --sample-size 10`. Status: submitted.
2026-05-24  codex   [gpt-5]     --        Maintainer directed CODEX-T and CODEX-U to stay in the same v0.8.0 scope. Updated `docs/agents/board.md` and `wiki/investigations/architecture-review-2026-05-18.md` so T/U are no longer described as v0.9.0 deferrals.
2026-05-24  codex   [gpt-5]     CODEX-J   Submitted: mechanically split `src/client.rs` into `client/batch_exec.rs`, `client/diagnostics.rs`, `client/schema_export.rs`, `client/string.rs`, and `client/subscriptions.rs` while preserving the `EipClient` facade. `src/client.rs` reduced from 6,758 to 4,515 lines. Verification passed: `cargo check --features ffi --locked`, route-path simulator tests, Clippy all targets/features, simulator tests, FFI state consistency test, full workspace tests, Python unittest discovery, and C# wrapper tests. Status: submitted.
2026-05-24  codex   [gpt-5]     CODEX-P   Submitted: added public actor-backed `Client` handle. The worker task owns `EipClient`; cloned handles submit read/write/batch/health/diagnostics commands over mpsc and receive oneshot replies, preserving single-owner wire access and request serialization. Existing `EipClient`, FFI, C#, and Python surfaces remain intact. Verification passed: actor simulator tests, simulator suite, Clippy all targets/features, Python tests, and C# wrapper tests. Status: submitted.
2026-05-24  codex   [gpt-5]     CODEX-R   Submitted: added `ConnectionEvent` and `Client::events()` broadcast stream from the actor handle, with simulator coverage for connected and terminal worker events. Status: submitted.
2026-05-24  codex   [gpt-5]     CODEX-Q   Submitted: added `EipClient` and actor `Client` service-layer helpers for STRING writes and UDT member / UDT-array-member read-modify-write flows, keeping the workaround concrete to documented Logix restrictions. Verification passed: `cargo check --features ffi --locked`, `cargo test --test client_actor_tests --locked`, and Clippy all targets/features. Status: submitted.
2026-05-24  codex   [gpt-5]     CODEX-S   Submitted: added `RetryPolicy`, `Backoff`, and `RetryClient` on the actor-backed client; retries use `EtherNetIpError::is_retriable()`, support constant/exponential backoff, and require explicit opt-in for writes. Verification passed: `cargo test --test client_actor_tests --locked` and Clippy all targets/features. Status: submitted.
2026-05-24  codex   [gpt-5]     CODEX-K   Submitted: completed release-window API cleanup: `#[non_exhaustive]` public enum sweep, typed `try_init_tracing` / `ProductionConfig` file I/O errors, typed logging config enums, crate-private connected-session wire state, private ordered-hop `RoutePath` storage, ordered-hop FFI exports plus C#/Python wrapper sync, and removal of duplicate string-specific error variants. Status: submitted.
2026-05-24  codex   [gpt-5]     CODEX-T   Submitted: added additive `Fleet<PlcId>` multi-PLC actor pool with per-PLC client handles, fleet-level health checks, and fleet-level forwarded connection events. Verification passed: `cargo test --test fleet_tests --locked` and `cargo check --features ffi --locked`. Status: submitted.
2026-05-24  codex   [gpt-5]     CODEX-U   Submitted: promoted shared value types, protocol codecs, tag-path parsing, and UDT helpers into sibling workspace crates (`rust-ethernet-ip-types`, `rust-ethernet-ip-protocol`, `rust-ethernet-ip-tag-path`, `rust-ethernet-ip-udt`). Main crate now depends on and re-exports/wraps those crates, with explicit error conversions back to `EtherNetIpError`. Status: submitted.
2026-05-24  claude  [Opus 4.7]  --        Baseline verification of the 9-brief submission: fmt + clippy --workspace --all-targets --all-features -D warnings clean; cargo test --workspace --all-features --locked → 236 passed / 0 failed (was 218; +18 from new sub-crate test bins, ffi_state_consistency, client_actor_tests, fleet_tests); dotnet test 79/79 after rebuilding cdylib --features ffi; Python unittest discover 35 pass + 8 skipped. Hardware regression via examples/test_plc_full_coverage.rs against ControlLogix: identical 2299/2299 reads, 1806/1806 verify — zero regressions from the structural overhaul.
2026-05-24  claude  [Opus 4.7]  CODEX-M   Phase B reviewed: implementation matches Claude-amended direction line-for-line. route_path → Arc<Mutex>, max_packet_size → Arc<AtomicU32>, session_handle/batch_config stay copied with documented invariants, compile-time Send+Sync+'static assertion, tests/ffi_state_consistency.rs 1000-iter loop passes. Bonus: eip_set_max_packet_size is no longer a stub. Zero defects. Status: merged.
2026-05-24  claude  [Opus 4.7]  CODEX-J   Reviewed: pure mechanical split, no behavior change. src/client.rs 6762 → 2369 LOC across new submodules. Hardware regression confirms zero behavior delta. Zero defects. Status: merged.
2026-05-24  claude  [Opus 4.7]  CODEX-P   Reviewed: additive `Client` actor handle alongside existing `EipClient` — preserves all FFI/wrapper contracts while exposing cancellation-safe cheap-clone concurrency model to Rust consumers who opt in. mpsc/oneshot actor pattern with WorkerStopped on terminal handle drop. 5/5 client_actor_tests pass. Two non-blocking polish notes (no backpressure surface, from_eip_client bypasses Connected event). Status: merged.
2026-05-24  claude  [Opus 4.7]  CODEX-Q   Reviewed: additive service methods write_string_tag, write_udt_member, write_udt_array_member package the read-modify-write dance for documented Logix firmware quirks. Stayed concrete per brief — no generalization. Future enhancement: thread through to C#/Python wrappers (separate brief). Status: merged.
2026-05-24  claude  [Opus 4.7]  CODEX-R   Reviewed: additive ConnectionEvent enum + Client::events() returning tokio broadcast Receiver. Events sourced from actor worker so state transitions are authoritative. Connected/WorkerStopped emit; Disconnected reserved for future reconnect path. Status: merged.
2026-05-24  claude  [Opus 4.7]  CODEX-S   Reviewed: additive RetryPolicy, Backoff, RetryClient on the actor surface. Builder/decorator pattern (client.with_retry(policy).read_tag()) reuses EtherNetIpError::is_retriable(). Reads retry by default; writes opt-in via retry_writes(true). Status: merged.
2026-05-24  claude  [Opus 4.7]  CODEX-K   Reviewed: code clean — all 9 #[non_exhaustive] enums + try_init_tracing typed return + ProductionConfig typed I/O + LogLevel/Format/Schedule enums + ConnectedSession/ConnectionParameters demoted to pub(crate) + RoutePath private storage + ordered-hop FFI exports + STRING error variant consolidation all landed. Status held at submitted: Cargo.toml still pinned at 0.8.0 but the changes are SemVer-major vs 0.7.0 baseline. Maintainer must pick: tag as 1.0.0 (recommended), hold K for later 1.0.0 cut, or accept SemVer break on 0.8.0 tag (not recommended — cargo-semver-checks will block).
2026-05-24  claude  [Opus 4.7]  CODEX-T   Reviewed: code clean — Fleet<PlcId> over actor-backed Client with per-PLC handles, fleet-level events, fleet-level health check. 3/3 fleet_tests pass. Status held: brief was an explicit v0.9.0 deferral; landing in v0.8.0 expands scope. Maintainer decision needed: accept (paired with K as 1.0.0) or defer to v0.9.0.
2026-05-24  claude  [Opus 4.7]  CODEX-U   Reviewed: workspace expanded to 4 sibling crates (rust-ethernet-ip-{types,protocol,tag-path,udt}). Main crate re-exports preserve the API surface. Status held: brief was a v0.9.0 deferral; also raises a release-process question (4 new crates.io entries to claim before publish). Maintainer decision needed: (a) full split + publish in this release, (b) accept structure but defer publish (sibling crates stay publish=false), or (c) hold and inline back.
2026-05-24  --      --        Maintainer release-strategy decisions: CODEX-K tag as 1.0.0 (option 1); CODEX-T accept paired with K as 1.0.0 (option 1); CODEX-U accept structure, defer publish — sibling crates marked publish = false (option 2). Release-prep applied: Cargo.toml main + 4 sibling crates bumped 0.8.0 → 1.0.0; sibling crates marked `publish = false`; main-crate path-dep pins bumped to 1.0.0; src/lib.rs head-doc release-line references bumped; CHANGELOG `[Unreleased]` promoted to `[1.0.0] - 2026-05-24` with an empty new `[Unreleased]` above. Build + 236 workspace tests + sim tests + dotnet 79/79 + Python 35/8-skipped all pass at 1.0.0. Hardware regression on full-coverage exerciser identical (2299/2299 reads, 1806/1806 verify). CODEX-K/T/U merged with the maintainer-amended scope; `git tag v1.0.0` + crates.io publish + NuGet publish flow remain maintainer-owned per the original gating items.
2026-05-24  --      --        Release-readiness review (Codex) caught four blockers missed in the 1.0.0 prep: (1) `cargo package -p rust-ethernet-ip` fails because the sibling crates are publish = false; (2) `VERSION`, `csharp/.../RustEtherNetIp.csproj`, and `python/pyproject.toml` still pinned at 0.8.0 so NuGet/Python packages would mis-identify; (3) `src/version.rs` `MAJOR/MINOR/PATCH_VERSION` constants were stale at 0.5.5 (pre-existing rot — owned and fixed now); (4) README and board still contained 0.8.0 gating language. Maintainer revised CODEX-U decision (option 2 → option 1: flip siblings to publishable). All four fixed and verified: each sibling now `cargo package`s cleanly; main crate still requires release-day publish ordering (types + tag-path → protocol + udt → main) which is normal Cargo workspace behavior. Wrapper ABI library-version assertions also bumped to 1.0.0 (caught earlier this run, committed in 559aec4). Cross-binding hardware re-run at 1.0.0: Rust 2299/2299/1806 (test-side classification 400 anomalies, not real); C# **2299/2299, 2206/2206, 2206/2206, 0 anomalies**; Python (with CODEX-W typed-write fix) **2299/2299, 2206/2206, 2206/2206, 0 anomalies**. v1.0.0 is now release-ready pending only the multi-hop ethernet hardware bench + crates.io name claims at tag time.
2026-05-24  --      --        Second release-readiness review (Codex) corrected Claude's overstatement: only `rust-ethernet-ip-types` and `-tag-path` `cargo package` cleanly at the local tree right now. `-protocol`, `-udt`, and main `rust-ethernet-ip` all fail with "no matching package found" until their workspace deps are actually on crates.io. That's expected Cargo behavior — not a configuration problem — but the prior log entry's "each sibling cargo packages cleanly" was sloppy. Also caught lingering `publish = false` drift in `board.md:11` (the manifests no longer have it but the summary still said so). Both fixed: gating item #2 in `board.md` now spells out the staged publish order (push → publish types + tag-path → wait for index → dry-run + publish protocol + udt → wait → dry-run + publish main → tag); board status snapshot at line 11 rewritten to remove the `publish = false` reference. Good-to-push for `main`: yes. Good-to-blindly-tag-and-publish: no — follow the staged sequence.
2026-05-24  --      --        **v1.0.0 released.** Codex executed the staged release sequence cleanly: 6 commits pushed to `origin/main` (head `f02eef5`); `cargo publish` ran in dependency order — `rust-ethernet-ip-types` → `rust-ethernet-ip-tag-path` → `rust-ethernet-ip-protocol` → `rust-ethernet-ip-udt` → `rust-ethernet-ip`, all at `1.0.0`; main-crate dry-run passed after sibling-crate index propagation; annotated tag `v1.0.0` created on `f02eef5` and pushed to origin (verified remotely). NuGet `RustEtherNetIp 1.0.0` ships via the tag-triggered GitHub release workflow (gh CLI not installed locally so workflow run not monitored — assumes `NUGET_API_KEY` is configured). Working tree clean. Single residual: multi-hop ethernet hardware validation remains a documented confidence upgrade (no wire-format change vs the validated paths, so not gating for 1.0.x).
2026-05-24  claude  [Opus 4.7]  --        Post-release docs sweep: README, docs/README.md, docs/SOFTWARE_ARCHITECTURE.md, docs/VERSION_MANAGEMENT.md, and docs/agents/board.md flipped from "1.0.0 release-candidate / draft" framing to "1.0.0 released" framing. Board "Gating items for v1.0.0 release" section rewritten as a completed release sequence checklist; "Post-1.0.0 confidence upgrades" section now carries only the multi-hop hardware bench item; Project context updated to record `v1.0.0` as the current released version and `1.0.x` as the active patch line.
2026-05-25  claude  [Opus 4.7]  CODEX-Z   Brief authored: validate agent task file frontmatter + board/log consistency on pre-commit. Walks `docs/agents/tasks/CODEX-*.md`, enforces required frontmatter keys (id, title, owner, status, created, last-update), validates status enum + date format + section presence + filename ↔ id match, cross-checks against `board.md` Open/Done tables and `log.md` parseability. Ships as `scripts/validate-agent-files` + `.githooks/pre-commit` + `scripts/install-hooks` + CI gate. Inspired by `steipete/agent-scripts` validate-skills pattern. Would have caught at least 3 drift bugs from the 1.0.0 release week. Highest-value of the four agent-infra briefs. Status: open.
2026-05-25  claude  [Opus 4.7]  CODEX-AA  Brief authored: release-readiness checker — version-string parity + cargo package chain. Single `scripts/check-release-readiness X.Y.Z` walks every version-string site (Cargo.toml + 4 sibling Cargo.toml + path-dep pins + VERSION + csproj + pyproject.toml + src/version.rs + src/lib.rs head doc + CHANGELOG + demo csproj files), asserts parity, runs `cargo package --no-verify` per workspace member in dep order. CI gate runs against current Cargo.toml version on every PR. Would have caught all 4 of the v1.0.0 release-readiness blockers the second-pass review found. Status: open.
2026-05-25  claude  [Opus 4.7]  CODEX-AB  Brief authored: structured Claude-review template — six-question contract + fixed output shape. Adopts the github-deep-review pattern from `steipete/agent-scripts`: every `## Claude review` section answers nine fixed sections in order (independent verification → what's being fixed → root cause → fix appropriateness → test proof → residual risk → strong points → findings → acceptance tally), file:line citations required, "not proven" preferred over speculation. Codifies template in CLAUDE.md + `docs/agents/review-template.md` with CODEX-W's review as the worked example. Length discipline: 60-line target, escape hatch via `wiki/investigations/`. Documentation-only brief. Status: open.
2026-05-25  claude  [Opus 4.7]  CODEX-AC  Brief authored: committer wrapper script — enforce specific-file staging + non-empty message. `scripts/agent-commit "msg" file1 [file2] ...` rejects `.`/`-A`/wildcards, validates message non-empty, pre-stages hygiene via `git restore --staged :/`, screens for obvious secret filename patterns, warns on subjects > 70 chars, blocks `--amend` by default. Lower priority than Z/AA (preventative rather than caught-real-bugs); the wrapper takes the discipline out of the human/agent loop. Inspired by the `committer` script in `steipete/agent-scripts`. Status: open.
2026-05-25  --      --        **Patch-release policy** (maintainer direction): post-1.0.0 changes accumulate on `main` without triggering a new crates.io / NuGet release. Agent-infra (CODEX-Z/AA/AB/AC), tooling, docs, and test-only work do NOT cut a patch. Next patch release (1.0.1) is queued for the first real library change — current candidates are CODEX-G, CODEX-H, CODEX-O. At that point the maintainer decides which accumulated changes ride the patch, runs the staged publish sequence per `board.md`, and tags. Until then: push to `main` freely, no release pressure. Documented at `board.md` line 16 and here.
2026-05-25  codex   [gpt-5]      CODEX-Z   Submitted: added `scripts/validate-agent-files`, `.githooks/pre-commit`, `scripts/install-hooks`, fixture-based smoke tests, docs, and CI gate for agent task frontmatter, section order, board consistency, and log parseability. Verification passed: validator on repo state and fixture smoke tests. Status: submitted.
2026-05-25  codex   [gpt-5]      CODEX-AA  Submitted: added manifest-driven `scripts/check-release-readiness`, release-readiness tests, VERSION_MANAGEMENT / CLAUDE docs, and CI job. Verification passed: manifest drift tests and `scripts/check-release-readiness 1.0.0`; package rows reported crates.io index unavailable notes in the sandbox unless `--strict` is used. Status: submitted.
2026-05-25  codex   [gpt-5]      CODEX-AB  Submitted: added `docs/agents/review-template.md` with the nine-section skeleton and worked CODEX-W-style example, plus CLAUDE / agent README pointers. Documentation-only. Status: submitted.
2026-05-25  codex   [gpt-5]      CODEX-AC  Submitted: added `scripts/agent-commit`, isolated temp-repo shell tests, CLAUDE / agent README docs, and CI job. Verification passed: `tests/agent_commit_tests.sh`. Status: submitted.
2026-05-25  claude  [Opus 4.7]  CODEX-Z   Reviewed: independent verification reproduced Codex's claims. Validator correctly exits 1 on missing board/log + on task ↔ board drift (initial appearance of silent-pass was a `$? after pipe` reading error on my side; confirmed via PIPESTATUS / no-pipe runs). Python over Bash was the right call (253 lines vs unreadable in pure Bash). Data-driven constants (`STATUSES`, `REQUIRED_KEYS`, `SECTION_ORDER`) make schema amendments cheap. Hook + CI gate both correctly invoke the script. 5 test fixtures cover every error class the brief listed; smoke runner asserts both exit code AND error fragment. Zero defects. Status: merged.
2026-05-25  claude  [Opus 4.7]  CODEX-AA  Reviewed: independent verification reproduced Codex's claims. Data-driven manifest (`scripts/check-release-readiness.txt`, 25 rows in pipe-delimited format) is exactly the right factoring per the brief — new sites are added there, not in the 125-line script body. `--strict` / `--ignore-examples` flags both present per acceptance criteria. CI gate has `continue-on-error: ${{ github.event_name == 'pull_request' }}` matching the CODEX-V cargo-semver-checks pattern. Would have caught all 4 v1.0.0 release-readiness blockers. Zero defects. Status: merged.
2026-05-25  claude  [Opus 4.7]  CODEX-AB  Reviewed: independent verification ran by using the new template for the CODEX-Z/AA/AB/AC reviews on this same commit — template is its own live test. Each fit within the ~60-line target without compression. Skeleton at `review-template.md:9-39` is fenced-code-block formatted so reviewers can copy literally. Severity prefix legend and "Not proven" phrasing both documented. One 🟡 polish note surfaced from real usage: "What's being fixed" prompt assumes a bug-fix shape — for new-infrastructure briefs (Z/AA/AC), reviewers end up writing "N/A — this is new infrastructure"; worth a one-line template accommodation in a future patch. Zero defects, zero real concerns. Status: merged.
2026-05-25  claude  [Opus 4.7]  CODEX-AC  Reviewed: independent verification confirmed all 8 brief validation steps present in `scripts/agent-commit` (154 lines). `--amend` explicitly rejected before `--amend-anyway` can be supplied (`agent-commit:30-37`), structurally enforcing the CLAUDE.md "never amend" rule. Wildcard rejection covers `.`, `-A`, `--all`, `*`. Pre-stage hygiene via `git restore --staged :/` matches the brief's "only-these-files-in-the-index" contract. Subject-length >70 warning is informational, not blocking. Test runner uses temp `git init` directories — no pollution risk. CI gate present. Preventative-tier brief landing cleanly. Zero defects. Status: merged.
2026-05-25  claude  [Opus 4.7]  CODEX-AD  Brief authored: fix Rust full-coverage classification + close the settle verification loop. Two-item brief: (1) flip `gTestUDT_Array[i].Array_{DINT,REAL,BOOL}[j]` (controller and program scope) from `WriteMode::FirmwareBlocked` to `WriteMode::Writeable` in `examples/test_plc_full_coverage.rs` so Rust matches the C# and Python runners' `0 anomalies` result on a healthy PLC; (2) add Phase 6 sample read-back of settled tags across all three runners so the Phase 5 settle is closed-loop verified. Small immediate fix; tooling-only; lands on `main` without cutting a release per the patch-release policy. Status: open.
2026-05-25  claude  [Opus 4.7]  CODEX-AE  Brief authored: cross-binding hardware harness — shared tag manifest + JSON output + granular firmware classification + preflight inventory check. Bundles Codex's recommendations #2-6 from the 2026-05-25 cross-binding parity review. Adds `examples/full_coverage_tags.json` as the single source of truth (refactors all three runners to consume it instead of duplicating the 2299-tag inventory), defines a writeability enum with 6 granular categories (replacing the over-broad single `FirmwareBlocked` bucket), emits per-run JSON results to `examples/full_coverage_results/`, runs a preflight tag-existence check before Phase 1 so PLC project errors surface as setup-errors (exit 2) rather than library regressions. CI gate runs `--dry-run` to validate manifest parses + tag list resolves per binding. Parallel-safe with CODEX-AD; AD lands first and AE inherits the correct classification. Tooling-only; no release trigger. Status: open.
2026-05-25  codex  [gpt-5]  CODEX-AD  Submitted: implemented the Rust full-coverage classification correction through the shared manifest and added Phase 6 settle read-back across Rust, C#, and Python. Dry-runs resolve matching counts in all bindings (`2299` tags, `2206` writeable, `74` firmware-blocked, `19` read-only). Verification passed: `cargo test --test plc_sim_tests --locked`, `cargo test --workspace --all-features --locked`, `cargo fmt -- --check`, and `cargo clippy --workspace --all-targets --all-features -- -D warnings`.
2026-05-25  codex  [gpt-5]  CODEX-AE  Submitted: added `examples/full_coverage_tags.json` as the single full-coverage manifest and refactored Rust/C#/Python exercisers to load it, run optional preflight, support dry-run, emit parity summary lines, and write JSON result artifacts with phase/category counters under `examples/full_coverage_results/`. Added manifest validation script, CI dry-run job, ignored generated result JSON, and linked the manifest from `docs/PLC_TEST_TAG_DEFINITIONS.md`. Verification passed: manifest test, all three dry-runs, C# build, Python py_compile, full Rust workspace tests, fmt, clippy, and diff check.
2026-05-25  codex  [gpt-5]  CODEX-AD  Hardware validation completed against ControlLogix `10.136.15.20:44818`, slot `0`, after the Rust classification and Phase 6 changes. Rust, C#, and Python each passed with reads `2299/2299`, writes `2206/2206`, verify `2206/2206`, Phase 6 settle verify `14/14`, blocked-as-expected `60`, and `0` anomalies.
2026-05-25  codex  [gpt-5]  CODEX-AE  Hardware validation completed against ControlLogix `10.136.15.20:44818`, slot `0`, after the shared-manifest refactor. Rust, C#, and Python each passed with preflight `2299/2299`; JSON result artifacts were generated under `examples/full_coverage_results/`.
2026-05-25  claude  [Opus 4.7]  CODEX-AD  Reviewed: hardware re-run confirmed `unexpected_anomalies=0` on the Rust runner (was `400` pre-fix). Classification correction landed via the AE manifest rather than as a discrete `WriteMode::FirmwareBlocked → Writeable` edit — bundling justified by the structural overlap. Phase 6 settle-verify lands across all three runners at 14/14, well under the 1-second target. 🟡 Cosmetic: Rust artifact uses Unix epoch seconds while C#/Python use ISO compact; future polish. Zero defects. Status: merged at `59a2176`.
2026-05-25  claude  [Opus 4.7]  CODEX-AE  Reviewed: schema design landed cleanly. `examples/full_coverage_tags.json` is the single source of truth (25 categories, the 6-variant writeability enum including reserved `service_layer_writeable` for future CODEX-Q wiring), all three runners consume it, JSON results emitted per binding, preflight uses exit 2 to distinguish PLC project errors from library regressions. CI gate runs manifest schema test + all three `--dry-run` invocations without network. Inspected all 3 JSON artifacts produced from the live hardware run — byte-identical per-phase counts across bindings (2299/2206/2206/14/60/0). Per-member writeability override is the right factoring for mixed-category groups. Manifest-as-data means future tag-inventory changes are one JSON edit, not three runner edits. Zero defects. Status: merged at `59a2176`.
2026-05-25  claude  [Opus 4.7]  --        Hardware validation extended to **CompactLogix 1769-L18ER-BB1B fw33** at `192.168.0.1:44818` slot 0 (integrated CPU+ethernet, no backplane bridge). Maintainer transplanted the same tag set from the ControlLogix L75. All three bindings ran the full-coverage exerciser cleanly: Rust/C#/Python each reported `reads=2299/2299 writes=2206/2206 verify=2206/2206 phase6=14 blocked=60 anomalies=0 RESULT=PASS`. Six JSON artifacts now under `examples/full_coverage_results/` (3 from L75 earlier today, 3 from L18ER just now), all with byte-identical per-phase counts. Single usability finding: C# runner resolves the manifest path relative to cwd, so it must be invoked from the repo root (e.g. `dotnet run --project examples/CSharpFullCoverage`) — Rust and Python have the same cwd-sensitivity. Not a library bug, pure example-harness ergonomics; worth a future polish brief. Validation evidence committed at `docs/validation/2026-05-25_real_plc_two-controller_cross-binding_full-coverage.md`. Per the patch-release policy, this is testing evidence — no library version change.
2026-05-25  claude  [Opus 4.7]  CODEX-AF  Brief authored: full-coverage exerciser — cwd-independent manifest resolution across all three bindings. Direct follow-up to the 🟡 polish finding from the 2026-05-25 CompactLogix L18ER validation: Rust uses `env!("CARGO_MANIFEST_DIR")`, C# adds `<None Include="..\..\examples\full_coverage_tags.json"><CopyToOutputDirectory>Always</CopyToOutputDirectory><Link>full_coverage_tags.json</Link></None>` so MSBuild copies the manifest next to the .dll for `AppContext.BaseDirectory` resolution, Python uses `Path(__file__).resolve().parents[2]`. `--manifest <path>` flag stays as explicit override. `tests/full_coverage_manifest_tests.sh` extended with cwd-independence cases (invoke from `/tmp`). Examples-only change; no library code, no version bump per patch-release policy. Status: open.
2026-05-25  codex  [gpt-5]  CODEX-AF  Submitted: made the full-coverage manifest resolution cwd-independent across Rust, C#, and Python while preserving explicit `--manifest` overrides. Added clean `manifest-error:` failures for bad paths, copied the manifest beside the C# runner at build time, expanded `tests/full_coverage_manifest_tests.sh` to exercise default/override/bad-manifest cases from `/tmp`, and updated the two-controller validation note to mark the usability finding closed. Verification passed: manifest cwd tests, fmt, clippy, C# build, and Python py_compile.
2026-05-25  claude  [Opus 4.7]  CODEX-AF  Reviewed: implementation matches the brief exactly across all three bindings. Rust uses `env!("CARGO_MANIFEST_DIR")`, C# uses `Path.Combine(AppContext.BaseDirectory, ...)` plus the load-bearing `<Link>` element in the csproj so MSBuild flattens the copy, Python uses `Path(__file__).resolve().parents[2]` with the documented walk-up comment. Each runner returns exit code 2 on bad manifest paths with a `manifest-error:` prefix — matches the CODEX-AE preflight contract. New test runner `full_coverage_manifest_cwd_tests` exercises 9 cases (3 bindings × default/override/bad-path) including a `assert_clean_manifest_error` helper that asserts both the `manifest-error:` prefix AND absence of stack traces. Live hand-repro from `/tmp` confirmed all three resolve correctly. Validation evidence file updated to mark the gap closed. Pre-existing `tests/cross_language_compatibility_tests.rs` failures surfaced because the live PLC at 192.168.0.1 now responds — those tests hardcode the IP and read legacy `TestTagController` tags, unrelated to CODEX-AF. Worth a future brief. Zero defects. Status: merged at `6ec3f8d`.
2026-05-25  claude  [Opus 4.7]  CODEX-AG  Brief authored: cross_language_compatibility_tests — honor SKIP_PLC_TESTS + TEST_PLC_ADDRESS, migrate to gTest* tag set. Follow-up to the pre-existing finding surfaced during CODEX-AF review: the file hardcodes `TEST_PLC_IP = "192.168.0.1:44818"` (`tests/cross_language_compatibility_tests.rs:7`), ignores `SKIP_PLC_TESTS`, reads legacy `TestTagController` (and similar) tag names that don't exist on the current `gTest*` PLC project layout, and only catches connection/timeout errors as graceful skips — tag-not-found errors panic. Brief: declare `mod test_helpers;`, call `should_skip_plc_tests()` at the top of each test, use `connect_to_plc(&get_test_plc_address(), 10)`, migrate `TestTag*` → `gTest*` names, treat CIP `0x04`/`0x05` (Path segment/destination unknown) as graceful skip with a clear log line. Tests-only landing, no library change, no version bump per the patch-release policy. Status: open.
2026-05-25  codex  [gpt-5]  CODEX-AG  Submitted: updated `cross_language_compatibility_tests` to honor `SKIP_PLC_TESTS`, use `TEST_PLC_ADDRESS` through `test_helpers`, read current `gTest*` tags, and gracefully skip only narrow CIP `0x04`/`0x05` tag-not-found setup errors. The required workspace skip-gate exposed the same legacy-env issue in `program_tag_tests` and `udt_enhanced_parsing_tests`, so those adjacent PLC-dependent tests were moved to the same helper/env-var/`gTest*` convention. Verification passed: focused skip/default tests, focused clippy, fmt, and `SKIP_PLC_TESTS=1 cargo test --workspace --all-features --locked`.
2026-05-25  claude  [Opus 4.7]  CODEX-AG  Reviewed: implementation matches the brief — `mod test_helpers;` declared, `should_skip_plc_tests()` early-return on each test, `connect_to_plc(&get_test_plc_address(), 10)` replaces the hardcoded `EipClient::connect("192.168.0.1:44818")`, six representative `gTest*` tags (DINT/REAL/BOOL/UDT-member/UDT-nested-array) replace the legacy `TestTag*` set, narrow `is_tag_not_found_error` matches only CIP `0x04`/`0x05`/`Path segment error`/`Path destination unknown` strings. Codex extended scope to `tests/program_tag_tests.rs` and `tests/udt_enhanced_parsing_tests.rs` — both carried the identical anti-pattern; expansion documented in Codex log and applies byte-identical helper shape. Verification: fmt clean, clippy clean, `SKIP_PLC_TESTS=1 cargo test --workspace --all-features --locked` green, `validate-agent-files` ok. Behavior matrix: row 1 (SKIP_PLC_TESTS=1) 5/5 ok in 0.00s; row 2 (unreachable addr) 10s timeout then ok; row 3 (live PLC at 192.168.0.1) 5/5 ok in 0.07s — wall-clock discriminates execute (0.07s) from skip (10s) against the same compiled binary; row 4 (tags missing) trusted via code inspection of match arms only. Zero defects. Tests-only landing, no library change, no version bump per patch-release policy. Status: merged at `fe5059c`.
2026-05-26  codex  [gpt-5]  CODEX-G  Submitted: implemented the post-1.0.0 local polish bundle except multi-chassis hardware validation. `plc_manager.rs` connection-pool unwraps now return `EtherNetIpError::Connection`; `PlcConfig::default` uses `SocketAddr::from`; removed `PlcManager::health_check_interval` and the dead BOOL-array `len >= 8` decode branch; replaced placeholder benchmarks with real protocol encode/decode benchmarks; added `PlcValue::known_data_type()` and made type-prefixed UDT encoding use symbol-derived type codes. Deferred `TagCache` deletion because it is publicly re-exported on the 1.0.x line. CI fix included: removed .NET preview channel from workflow setup and updated C# test runner packages after GitHub run `26424069105` showed Ubuntu C# tests passing then crashing during testhost shutdown. Verification passed: fmt, clippy, protocol/types tests, lib tests, benchmark compile, release FFI build, C# tests 79/79, and full workspace tests outside sandbox after simulator bind approval. Also covers CODEX-H, CODEX-I, and CODEX-O. Status: submitted.
2026-05-26  claude  [Opus 4.7]  CODEX-G  Reviewed (bundled with CODEX-H partial, CODEX-I, CODEX-O at commit `2690669`). Independent verification: `cargo fmt --all -- --check` clean; `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean; `SKIP_PLC_TESTS=1 cargo test --workspace --all-features --locked` 238 passed / 0 failed / 52 ignored (+2 vs prior gate, accounting for the two new wire tests `unknown_udt_type_remains_generic_structure_marker` and `udt_type_prefixed_encode_uses_symbol_derived_type`); `cargo bench --no-run --locked` compiles cleanly; `scripts/validate-agent-files` ok; local C# tests with bumped runner stack (`Microsoft.NET.Test.Sdk 18.5.1 / xunit 2.9.3 / xunit.runner.visualstudio 3.1.5`) pass 79/79 on .NET 10 in 1s. CODEX-G: `src/plc_manager.rs` connection-pool unwraps replaced with `EtherNetIpError::Connection(...)` returns; restructured to compute `max_connections`/`max_packet_size` upfront avoiding borrow conflicts; `PlcConfig::default` uses `SocketAddr::from(([127,0,0,1], 44818))` instead of a runtime parse. Verified all 5 unwraps from the original brief audit are gone. CODEX-H partial: `PlcManager::health_check_interval` field removed (was initialized never read); `BOOL_ARRAY_DWORD` dead `else if value_data.len() >= 8` branch in `crates/protocol/src/values.rs:158-176` removed. `TagCache` deferred because it's publicly re-exported on the 1.0.x line and removal is SemVer-major; `src/client.rs` allow-list audit (9 sites) explicitly deferred — kept as remaining backlog. Acceptable scope split. CODEX-I: `benches/performance_benchmark.rs` replaced placeholders with real benchmarks exercising `PlcValue::encode`/`decode_payload` (5 type fixtures), `EncapsulationHeader::encode`, `CipRequest::encode` (5/10/25/50/100 batched READ/WRITE_TAG mix), and now references the actual codec boundary established by CODEX-D. CODEX-O (the load-bearing one): added `PlcValue::known_data_type() -> Option<u16>` to `crates/types/src/lib.rs`; `get_data_type()` keeps its existing `u16` signature for backward compat and unwraps to `0x00A0`; `crates/protocol/src/values.rs::write_data_type` now routes UDTs through `known_data_type` so type-prefixed UDT encoding emits `0x02A0 + symbol_id` (matching the AB convention) instead of the generic `0x00A0` placeholder. **Wire-protocol change**: type-prefixed UDT writes now send the correct symbol-derived type code, which is the documented AB write contract — previously sending `0x00A0` would have been firmware-rejected. The `symbol_id == 0` edge case (UDT constructed without a prior read) falls back to `0x00A0` and is covered by the new `unknown_udt_type_remains_generic_structure_marker` test. Existing `write_data_type_uses_udt_symbol_id` test still asserts `0x14D4` for `symbol_id = 0x1234` — backward compatibility for the common UDT RMW path is preserved. CI follow-up (not in any brief but bundled): `dotnet-quality: 'preview'` dropped from test/package/build jobs in `.github/workflows/ci.yml` (kept on the full-coverage-manifest job per maintainer edit); test runner packages bumped to versions known to work on .NET 10 GA. Investigation evidence at `wiki/investigations/dotnet-testhost-shutdown-2026-05-26.md`. **Process note**: no individual brief files were authored for G/H/I/O — the board's "Post-1.0.0 polish" inline summaries served as the contract. Acceptable for backlog cleanup work; review used the board entries as acceptance criteria. **Residual risk**: the wire change in CODEX-O has not been hardware re-validated; the previous 2026-05-25 cross-binding validation predates this commit. Maintainer-owned hardware re-run is queued before the 1.0.1 publish (user direction: accumulate more before cutting). Zero defects, zero Claude-applied fixes. Status: merged at `2690669`.
2026-05-26  claude  [Opus 4.7]  --  Hardware re-validation: full-coverage exerciser re-run against CompactLogix 1769-L18ER-BB1B at 192.168.0.1 after the CODEX-G/H/I/O bundle merged at `2690669`. All three bindings (Rust/C#/Python) PASSED with byte-identical matrices: 2299/2299 reads, 2206/2206 writes, 2206/2206 verify, 60 blocked_as_expected, 0 unexpected_anomalies — matches the 2026-05-25 baseline exactly. Confirms the CODEX-O wire change (type-prefixed UDT writes now emit 0x02A0+symbol_id) is wire-compatible with the existing UDT RMW workflow. Hardware gate for any 1.0.1 publish is cleared on the L18ER; L75 re-run still queued for when both controllers are bench-available. Evidence: `docs/validation/2026-05-26_post-codex-ghio-bundle_l18er_full-coverage.md`. JSON artifacts under gitignored `examples/full_coverage_results/`.
2026-05-29  claude  [Opus 4.7]  CODEX-AH  Brief authored: bump workspace MSRV from 1.95 → 1.96 and adopt `std::assert_matches::assert_matches!` at the 9 existing `assert!(matches!(...))` test sites for better failure diagnostics. Rust 1.96 shipped 2026-05-28; codebase analysis found `assert_matches!` is the only new API with real applicability (Copy ranges, `From<T> for LazyLock`, wasm/s390x/loongarch updates, and the cargo git+registry feature are all not relevant here). MSRV bump touches 5 Cargo.toml files, the dedicated CI MSRV job (`.github/workflows/ci.yml:119-124`, currently pinned to `@1.95.0`), the README badge, the CLAUDE.md MSRV line, and the python expansion prompt doc. Borderline case at `tests/plc_sim_tests.rs:309` (assert with custom message) flagged for Codex's call. No SemVer-major impact; per the patch-release policy this lands on `main` and the maintainer decides whether to fold the MSRV bump into 1.0.1 or escalate to 1.1.0 at release time. Status: open.
2026-05-29  codex  [GPT-5]  CODEX-AH  Implemented MSRV bump from 1.95 to 1.96 across all five Cargo.toml manifests, the CI MSRV job, README badge, CLAUDE.md, and the Python expansion prompt. Converted the 9 requested test `assert!(matches!(...))` call sites to `assert_matches!` using the stable Rust 1.96 `use std::assert_matches;` import, keeping imports in test-only scopes. Left the custom-message transport assertion in `tests/plc_sim_tests.rs` as `assert!(matches!(...), "...")` to preserve the existing diagnostic text. Verification: `cargo fmt -- --check` pass; `cargo clippy --workspace --all-targets --all-features -- -D warnings` pass; `SKIP_PLC_TESTS=1 cargo test --workspace --all-features --locked` pass outside sandbox after local simulator socket binding was denied inside sandbox; `cargo test --test plc_sim_tests` pass outside sandbox; `cargo check --workspace` pass with local stable `rustc 1.96.0`. Status: submitted for review.
2026-05-29  claude  [Opus 4.7]  CODEX-AH  Reviewed and merged at `e8e336b`. Independent verification on local `rustc 1.96.0 (ac68faa20 2026-05-25)`: fmt clean, clippy clean, `SKIP_PLC_TESTS=1 cargo test --workspace --all-features --locked` 238/0/52 (byte-identical to the post-CODEX-G/H/I/O baseline at `2690669`, confirming pure syntax conversion with zero semantic drift), `plc_sim_tests` 13/13 including the converted `simulated_plc_nested_bool_array_element_read_write` site, `validate-agent-files` ok. MSRV bumped at every brief-listed touchpoint (5 Cargo.toml + CI MSRV job + README badge + CLAUDE.md + Python expansion prompt). All 9 `assert!(matches!(...))` sites converted; `assert_matches` imports gated to `#[cfg(test)]` scope per acceptance criterion 7 — zero production-module footprint. The borderline `tests/plc_sim_tests.rs:310` site was left as `assert!(matches!(...), "msg")` per option (a) — preserves the custom failure message; documented in the Codex log. **Brief error owned by Claude:** the brief prescribed `use std::assert_matches::assert_matches;`; Codex correctly used the idiomatic shorter `use std::assert_matches;` (stdlib exposes the macro at the bare module path). Both behave identically. No wire/FFI change, no hardware re-validation required. Per the patch-release policy, the MSRV bump accumulates on `main`; maintainer decides at release time whether `1.0.1` (patch) or `1.1.0` (minor) is appropriate for the MSRV change. Zero defects, zero Claude-applied fixes. Status: merged.
2026-05-29  claude  [Opus 4.8]  --  CI fix: scoped the C# test step (and its cdylib build + .NET setup) to `matrix.rust == 'stable'` in `.github/workflows/ci.yml`. The `Test (ubuntu-latest / beta)` job's .NET testhost was intermittently aborting during shutdown after all run C# tests passed (varying pass-counts 31/52/64, no managed stack or panic text); the beta `cargo test` step always passed, isolating the fault to the beta-built `cdylib` racing the leaked global Tokio runtime threads at Linux testhost teardown. C# wrapper exercises a stable C ABI, so a stable-built cdylib is sufficient and the Rust paths stay covered by `cargo test` on beta — no coverage lost. Added `--blame-crash` to the surviving stable invocation for future dump capture. Updated `wiki/investigations/dotnet-testhost-shutdown-2026-05-26.md` with the recurrence, beta-scoping evidence, and applied fix. Not reproducible on macOS arm64 (full suite 13× clean). Verified: ci.yml YAML parses, `dotnet test ... --blame-crash` runs 79/79 locally.
2026-06-19  claude  [Opus 4.8]  --  Post-1.0.0 full review + fix pass (maintainer-driven, no individual briefs). Reviewed the project file-by-file (core Rust, C#/Python wrappers, examples, docs, build/CI) via parallel review agents, then implemented a three-tier bundle on branch `review-fixes-v1.1`. Tier 1 (correctness): bit access reworked to client-side read-modify-write (the prior `0x29`+1-byte bit segment was a malformed 16-bit logical-member marker) — hardware-validated; C# typed UDT writes serialize `data` as a byte array matching native `Vec<u8>` (was base64, silently rejected) and `eip_write_udt` accepts the `{symbol_id,data}` shape; C# failed-write no longer re-issues the write (double-apply); C# finalizer + thread-safe Dispose; `RoutePath::add_port`/`AddPort` no longer drops a port applied before the address (Rust+C#); Python headerless-UDT→STRING guard; subscription `change_threshold` documented as absolute deadband; `TagManager` tag-list item-count offset fix; `build.rs`/`version.rs` tolerate a no-git tarball build; README install 0.7.0→1.1.0; Python wheel now bundles the native lib (platform-tagged). Tier 2 (debt): docs MSRV sweep + crates-map + historical banners + new API_STABILITY/MIGRATION docs; deleted the unrunnable `examples/rust_examples/` tree; purged vestigial dead code in `client.rs`; moved the standalone `enip-test` harness out of the published crate; removed a write-only `ProductionMonitor` field; Docker/bench/build-script drift fixed. Tier 3 (features): `thiserror` 2.0, dropped unused `log`/`env_logger`, trimmed tokio features; the 22 scalar FFI wrappers macro-generated (ABI-identical, verified by Rust/C#/Python suites); new `eip_get_last_error` + capability `CAP_LAST_ERROR` surfaces the CIP cause through C# `PlcException` and Python `PlcOperationError`; C# Task.Run async API; release automation for crates.io (ordered ×5), PyPI (per-platform wheels), and multi-RID NuGet. Deferred with justification: FFI registry `Arc<Mutex>` refactor (reviews: correct today), `TagCache`/`PlcConnection::update_health` removal (SemVer-major), data-type-table dedup. Bumped to 1.1.0 across all 22 readiness-checked version locations with a CHANGELOG entry; verification green (fmt, clippy `-D warnings` all-features, full workspace tests, 85 C# tests, 31 Python tests). Not pushed — local branch awaiting maintainer merge/tag.
2026-06-19  claude  [Opus 4.8]  --  Hardware full-coverage validation of the 1.1.0 branch against CompactLogix 5069-L330ERM (firmware 38) at 192.168.0.101, slot 0. All three bindings (Rust/C#/Python) PASSED with byte-identical matrices: 2299/2299 reads, 2206/2206 writes, 2206/2206 verify, 60 blocked_as_expected (firmware-limited direct UDT-array-element-member writes, CIP 0x2107), 0 unexpected_anomalies — matching the prior L18ER baseline. Confirms the bit-access rewrite and the FFI scalar-macro refactor are wire-compatible end-to-end. The new `eip_get_last_error` path verified live (a bad read surfaces "CIP Error 0x04: Path segment error"). JSON artifacts under `examples/full_coverage_results/`.
2026-06-19  claude  [Opus 4.8]  --  1.1.0 shipped. Merged `review-fixes-v1.1` to `main` (fast-forward), tagged `v1.1.0`, pushed. Published to all three registries: crates.io (5 crates: types/tag-path/protocol/udt/rust-ethernet-ip, published locally in dependency order after the maintainer supplied a fresh token), NuGet `RustEtherNetIp 1.1.0` (multi-RID win-x64/linux-x64/osx-arm64, via the tag/dispatch release workflow + NUGET_API_KEY), and PyPI `rust-ethernet-ip 1.1.0` (win_amd64 + macOS wheels + sdist; Linux wheel deferred — see CODEX-AI). Post-tag fixes folded into `main`: AbiContractTests pinned library-version 1.0.0->1.1.0 (added to the release-readiness manifest), README + C# wrapper README version sweep, release.yml hardened (dropped the unreliable macOS-13 Intel runner + 20-min build timeout + workflow_dispatch publish + multi-RID NuGet .so staging), and a CI determinism fix for the C# native-lib resolution (the Ubuntu leg intermittently loaded a cached non-ffi cdylib -> EntryPointNotFound for eip_connect; now rm+rebuild fresh `--features ffi` and pin RUST_ETHERNET_IP_NATIVE_LIB). CI green on ubuntu/windows/macOS. Token-hygiene note: the maintainer pasted the crates.io and PyPI tokens in chat; advised rotation.
2026-06-19  claude  [Opus 4.8]  CODEX-AI  Brief authored: publish a manylinux Linux x86_64 Python wheel to PyPI. 1.1.0 shipped Windows/macOS wheels + sdist but no Linux wheel — the prebuilt-cdylib + setuptools layout fails auditwheel (`shared library in purelib`), so `linux-x64` is omitted from release.yml's pypi-wheels matrix and Linux `pip install` falls back to the native-lib-less sdist. Brief covers building/repairing a `manylinux_*_x86_64` wheel (preferably inside a manylinux_2_28 container so the glibc floor is portable), re-adding linux-x64 to the matrix, and a CI install-and-import smoke. Lands in the next release; 1.1.0 on PyPI is immutable. Status: open.
2026-06-19  claude  [Opus 4.8]  CODEX-AI  Implemented + merged at `98fc460` (Claude-implemented at maintainer direction). Linux x86_64 manylinux PyPI wheel now ships. Root cause diagnosed locally without Docker: the wheel had `Root-Is-Purelib: false` but the bundled native lib was routed to the `.data/purelib/` install scheme (auditwheel rejects: "shared library in purelib folder"). Fix: `install_lib = install_platlib` override in `python/setup.py` so the lib lands at the package root (platlib); verified on a local Windows wheel. Pipeline: `release.yml` `pypi-linux-wheel` builds the cdylib + wheel inside `quay.io/pypa/manylinux_2_28_x86_64` and auditwheel-repairs it; blocking `pypi-linux-smoke` installs the repaired wheel in a clean `python:3.12-slim` and asserts manylinux tag + import + `load_native_library()`; `pypi-publish` now needs the smoke gate. Dispatched at 1.1.0: manylinux build (1m27s) + smoke (9s) passed; `twine --skip-existing` added `rust_ethernet_ip-1.1.0-py3-none-manylinux_2_28_x86_64.whl` to the existing PyPI 1.1.0 release (no version bump). Confirmed live on the PyPI simple index. `pip install rust-ethernet-ip` now works on Linux x86_64. Out of scope (by design): aarch64/musl Linux wheels. Status: merged.
2026-06-19  claude  [Opus 4.8]  --  Created `docs/ROADMAP.md` as the canonical post-1.1.0 future-work backlog (per maintainer request), grouped by target release: 1.2.0 minor (full documentation refresh [priority], Linux aarch64 + macOS-Intel wheels, data-type-table dedup, FFI registry Arc<Mutex>), 2.0.0 major (remove dead public TagCache / PlcConnection::update_health, optional C# true-async FFI), and validation/ops (multi-chassis Ethernet routing hardware validation). Board `Next agenda` now points to ROADMAP.md as source of truth, superseding the stale 1.0.0-era candidate entries. Token rotation deliberately deferred by the maintainer for now.
2026-07-01  claude  [Fable 5]  --  Full repository analysis authored at `docs/agents/repo-analysis-2026-07-01.md` (maintainer-requested). Four parallel deep-review passes (core protocol/client, FFI + bindings, peripheral modules, project health) with every critical/high finding re-verified by direct source read; independent verification matrix green (fmt, clippy all-features -D warnings, full workspace tests). Headlines: C# `WriteUdtMember` deadlocks on every real call (mock-only tests hide it); the hand-rolled byte-emission paths in `client.rs`/`client/string.rs` contain multiple never-worked public APIs while everything routed through `CipRequest`/`CipResponse` is clean; the test simulator is client-derived rather than spec-derived (element-count injection at `tests/plc_sim.rs:733`) so two pinned byte tests pin wrong encodings; `discover_udt_members` fabricates UDT definitions; stream desync after timeout can misattribute responses; the cargo-semver-checks gate still baselines 0.7.0; CLAUDE.md architecture/build docs are two refactors stale. One conflict flagged honestly: the analysis's UDT write-encoding finding collides with CODEX-O's hardware validation — resolution is capture-gated (CODEX-AO), not code-first.
2026-07-01  claude  [Fable 5]  CODEX-AJ  Brief authored: C# wrapper critical fixes — WriteUdtMember nested-SemaphoreSlim deadlock (Core/locked split per the ReadUdtWithChunkedFallback pattern), ANSI→UTF-8 marshalling, keep-alive operation-lock serialization, connect-failure diagnostics; plus a new simulator-backed integration test project exercising the real P/Invoke boundary in CI (the missing layer that let the deadlock ship). Status: open.
2026-07-01  claude  [Fable 5]  CODEX-AK  Brief authored: hygiene hotfix bundle — semver-checks baseline 0.7.0→1.1.0, `vergen` out of runtime deps, `libc` optional behind `ffi`, authors placeholder email, release publish error discrimination, scheduled cargo-audit, delete never-compiled `tests/unit/` + `test_isolated/`, wire `release_readiness_tests.sh` into CI, README osx-x64 RID correction, CLAUDE.md `--features ffi` build-command fix, csproj `runtimes/{rid}/native/` layout. Status: open.
2026-07-01  claude  [Fable 5]  CODEX-AL  Brief authored: transport & session hardening — poison-on-timeout stream invalidation (fixes response misattribution), sender-context stamping/verification, `session_handle` moved to Arc-shared state (removes the FFI `store_client` re-insert race), `register_session` single-lock + proper framing. Protocol-touching: maintainer hardware smoke required before release. Status: open.
2026-07-01  claude  [Fable 5]  CODEX-AM  Brief authored: five addressing fixes, all sim-verifiable — `write_tag` drops `.Member` after `Array[i]`, `"Tag.15"` bit syntax operates on the whole word in read_tag/write_tag, batch BOOL array element reads miss the DWORD/32 mapping (the gap CODEX-X scoped out), malformed `.DATA[i]` element segment in tag-path, `discover_program_tags` ignores the program name. Status: open.
2026-07-01  claude  [Fable 5]  CODEX-AN  Brief authored: `read_array_range` offset-8 parse vs the spec's offset-6 replies, `get_tag_attributes` missing path-size + misaligned response walk, and the process fix — remove the simulator's client-shaped element-count injection so the sim is an oracle, with spec citations on every response builder. Step zero is reconciling against the full-coverage hardware evidence; hardware smoke + capture before release. Status: open.
2026-07-01  claude  [Fable 5]  CODEX-AO  Brief authored: capture-gated UDT wire-format investigation — resolves the analysis-vs-CODEX-O conflict on `0x02A0 + symbol_id` write encoding and the read path's structure-handle handling, with a maintainer capture checklist as the arbiter. Phase 1 (lands immediately): `crates/udt` strictness — `to_hash_map`/`from_hash_map` silent-skip/zero-fill can turn a partial read into a zero-filling RMW write; empty template member names shift the name pairing; packed BOOL members ignore the `info` bit index. Status: open.
2026-07-01  claude  [Fable 5]  CODEX-AP  Brief authored: retire the string/UDT strategy graveyard — `write_string`, `write_ab_string_udt` (unconditional Ok), connected messaging/Forward Open (dead on arrival), `write_string_unconnected`, `read_udt_chunked` strategies B–D (invalid requests, non-terminating loops, error-as-success), `*_by_offset` methods (index into the reply envelope). Deprecate + honest `Unsupported` errors on 1.x per the `eip_get_tag_metadata` precedent; deletion queued for 2.0; per-item reproduce-before-retire required. Status: open.
2026-07-01  claude  [Fable 5]  CODEX-AQ  Brief authored: dead-stratum deprecation — `discover_udt_members` re-routed from the fabricating TagManager parser (off-by-one path size pinned wrong by its own tests, invented `Member_N` definitions) to the real template path; discovery filter fixes (`validate_tag_name` rejects legal names, `is_structure` misses 0x02A0-class handles); deprecate `ProductionMonitor`/`ProductionConfig`/`PlcManager`/`SubscriptionManager`/`TagCache`; FFI diagnostics stop reporting hardcoded zero counters as health; fake `udt_discovery_benchmark` deleted or made real. Extends ROADMAP items 5–9. Status: open.
2026-07-01  claude  [Fable 5]  CODEX-AR  Brief authored: subscription/fleet/event lifecycle — `stop()` actually stops polling; abandoned consumers can no longer deadlock poll tasks (try_send/drop-oldest replaces blocking send into a channel that can never disconnect); transient read errors emit events instead of silently killing the subscription; unsubscribe + registry eviction; fleet forwarding survives `broadcast::Lagged` and replaced clients don't ghost-emit; `Client::events()` side-effect removed; `start_monitoring` gets a stop guard. Status: open.
2026-07-01  claude  [Fable 5]  CODEX-AS  Brief authored: FFI polish — the three raw `*mut EipClient` ABI exports made private, `catch_unwind` shim in the dispatch macro (panic = abort of the host process today), SAFETY-comment/doc pass restoring the ffi-safety.md contract (file-level clippy allow removed), last-error lifecycle (stale/unset/leaking today), `eip_write_udt` empty-payload and `eip_read_string` ASCII-scan fallbacks become errors, discover-detailed malloc-failure out-param hardening, id-wraparound guard, unregister-on-disconnect; Python finalizer/disconnect-wedge/library-search residuals. Sequenced after CODEX-AL. Status: open.
2026-07-02  claude  [Fable 5]  --  Hardware probe (maintainer-authorized, 5069-L330ERM fw38 at 192.168.0.101): the ab-firmware-quirks.md claim that direct STRING writes are firmware-blocked is a misdiagnosis. A Write Tag with the Logix structure encoding (`A0 02 CE 0F` + element count + 88-byte payload: LEN u32 + DATA[82] + 2 pad) succeeds on controller-scoped `gTest_STRING`, verified by read-back; the wired path's atomic `0x00CE`/unpadded shape is what draws extended error 0x2107, which is the documented Data Access "tag type mismatch", not a vendor/composite error. Maintainer-proposed `.LEN`/`.DATA` component writes also work (both via public `write_tag` per-byte and as a 2-round-trip raw `.DATA[82]`+`.LEN` sequence). STRING reads on hardware return `PlcValue::Udt` with the raw `CE 0F`-prefixed structure (sim returns atomic 0x00CE → decodes to String — sim/hardware divergence). Tag restored to its original value. Evidence: `docs/validation/2026-07-02_string_write_probe_5069-L330ERM_fw38.md`. Second hardware-overturns-analysis precedent after CODEX-O.
2026-07-02  claude  [Fable 5]  CODEX-AT  Brief authored: STRING wire format remediation grounded in the 2026-07-02 hardware evidence — write path emits the structure encoding (`A0 02 CE 0F`, 88-byte payload) from `write_tag(PlcValue::String)` and `encode_type_prefixed` (batch); standard-STRING reads (handle 0x0FCE) decode to `PlcValue::String` with `Udt` fallback for other handles; simulator becomes hardware-shaped for STRING (rejects the old atomic write shape with 0xFF/0x2107); 0x2107 error text corrected to "tag type mismatch"; ab-firmware-quirks.md STRING section rewritten against the validation doc; `full_coverage_tags.json` plain-STRING entries relabeled writable (UDT-member labels untouched pending hardware). Answers CODEX-AP item 5's decision point; coordinates with AM/AN/AP on shared files; maintainer full-coverage re-run gates the relabel. Status: open.
2026-07-02  claude  [Fable 5]  CODEX-AU  Brief authored: C++ consumer support for the maintainer's C++/Qt vision-system app — checked-in `include/rust_ethernet_ip.h` covering the FFI surface (excluding the raw-pointer trio CODEX-AS privatizes) with a two-direction CI parity gate (header→dylib via link check, dylib→header via export-table script); `examples/cpp/` CMake RAII wrapper + console demo with a ctest smoke against the simulator (ubuntu + windows CI legs); `docs/CPP_INTEGRATION.md` with the blocking-FFI threading model and Qt QThread-worker pattern; README section. Packaging-only, no `src/ffi.rs` behavior changes; sequence after CODEX-AS if in flight. Status: open.
2026-07-02  codex  [GPT-5]  CODEX-AK  Implementation started per maintainer sequence AK → AJ → AT → AM → AN → AL → AS → AP → AQ → AR → AU. Authors metadata decision confirmed: omit authors from all five manifests. Status: in-progress.
2026-07-02  codex  [GPT-5]  CODEX-AK  Submitted: semver-checks baseline moved to 1.1.0; root placeholder authors removed; runtime vergen removed; libc made ffi-optional; release publish now fails non-duplicate errors; audit scheduled weekly; dead tests/unit and test_isolated scratch files removed; release_readiness_tests wired into CI; README/CLAUDE/C# README docs corrected; local NuGet pack now places the native library under runtimes/<rid>/native. Verification passed including fmt, clippy all-features, workspace all-features tests with SKIP_PLC_TESTS=1, plc_sim_tests, semver-checks against 1.1.0, release-readiness checks, dotnet pack layout inspection, C# tests, Python tests, validate-agent-files, and diff-check. Status: submitted.
2026-07-02  claude  [Fable 5]  --  1.2.0 release plan recorded on the board (maintainer direction): CODEX-AJ…AU accumulate on `main` with no interim releases; full local matrix + maintainer hardware full-coverage pass gates the release (post-AT expected matrix: 58 blocked / 2208 writes / 0 anomalies); next version is 1.2.0 (minor — behavioral fixes, deprecations, additive surface; 2.0 deletions stay queued per ROADMAP). Recommended order AK → AJ → AT → AM → AN → AL → AS → AP → AQ → AR → AU, AO phase 1 interleaves freely, AO phase 2 captures bundled into the pre-release hardware session. Open-question resolutions relayed to Codex: AK item 4 = omit `authors` from all five manifests (sibling convention); AU sequences after AS. Board `Project context` refreshed (current released version was stale at v1.0.0; now v1.1.0).
2026-07-02  codex  [GPT-5]  CODEX-AJ  Submitted: C# wrapper now uses UTF-8 marshalling throughout `csharp/RustEtherNetIp/`; `WriteUdtMember` uses lock-free UDT core helpers inside a single operation lock; keep-alive health/reconnect now serializes with user operations and skips busy ticks; connect failures populate additive `LastConnectError`; new simulator-backed `RustEtherNetIp.IntegrationTests` project exercises real P/Invoke connect/disconnect, scalar and UTF-8 STRING round trips, native batch read/write, connect diagnostics, `WriteUdtMember` watchdog completion, and keep-alive contention. Standalone `plc_sim` gained raw UDT and Multiple Service Packet support. Verification passed: fmt, clippy all-targets/all-features, workspace all-features tests with `SKIP_PLC_TESTS=1`, release ffi build, C# unit tests, C# native integration tests, validate-agent-files, grep-clean ANSI check, and diff-check. Status: submitted.
2026-07-02  claude  [Fable 5]  CODEX-AK  Reviewed and merged at `2cf2d96` (bundled with CODEX-AJ — both submitted from one working tree sharing ci.yml/CHANGELOG; CODEX-G/H/I/O precedent). Independent verification green: fmt, clippy all-features -D warnings, workspace tests 247/0/52, plc_sim_tests 13/13, builds with/without ffi, `cargo tree` proves libc absent from the default graph, semver-checks vs 1.1.0 (223 pass), dotnet pack layout `runtimes/win-x64/native/` inspected, C# 86/86, both workflow YAMLs parse, all acceptance greps clean. All 11 items done; authors resolved as omit-entirely per maintainer direction. Findings: 🟡 the weekly `schedule:` fires the whole workflow (workflow-level trigger constraint) — accepted as toolchain canary; 🟢 readiness shell test needs UTF-8 stdout on Windows (env artifact, CI leg is ubuntu); 🟢 the flagged `data_types_showcase` all-targets failures are pre-existing stale STRING expectations — folded into CODEX-AT's brief. One Claude-applied fix: two-line CHANGELOG entry for the pre-existing `futures` default-features trim (`47b99a4`) per the brief's maintainer-consent clause. Zero defects.
2026-07-02  claude  [Fable 5]  CODEX-AJ  Reviewed and merged at `2cf2d96` (bundled with CODEX-AK). Independent verification on the combined tree: fmt/clippy/workspace 247/0/52, plc_sim_tests 13/13, release ffi build, C# unit 86/86, new integration suite 7/7 over real P/Invoke against a spawned plc_sim (watchdog + 200-read/50ms keep-alive stress included), Python suite OK, ANSI-marshalling grep clean, sln lists the new project. Deadlock fix verified as the brief's prescribed Core/locked split (`ReadUdtCore`/`WriteUdtCore`, single lock acquisition per public call); keep-alive tick now takes `_operationLock` with 100ms skip-on-contention and reconnects under the held lock; `Disconnect` stops/joins keep-alive before locking; `LastConnectError` discriminates -2 runtime-init. Acceptance deviation accepted with justification: the pre-fix watchdog demonstration was skipped (new test code cannot compile against pre-fix source; nested `SemaphoreSlim(1,1)` wait verified by inspection). Deferred: three-OS CI proof lands on next push's Actions run — watch it; sim-spawn flakiness on macOS/ubuntu would be a follow-up, not a reopen. Additive API noted: `LastConnectError`, settable `KeepAliveInterval`. Zero Claude-applied fixes, zero defects.
2026-07-02  codex  [GPT-5]  CODEX-AT  Submitted: standard Logix STRING write/read wire format implemented from the 2026-07-02 hardware probe. Single and batch STRING writes now emit structure marker `0x02A0` + standard STRING handle `0x0FCE`, element count 1, and the 88-byte `LEN u32 + DATA[82] + 2 pad` payload; standard STRING structure reads decode to `PlcValue::String` while unknown handles remain `Udt`. Both simulators now serve hardware-shaped STRING reads and reject the old atomic `0x00CE` write request with `0xFF/0x2107`; pinned tests cover request bytes, type-prefixed encoding, hardware-shaped decode, truncation error, UDT fallback, single/batch sim round-trips, short-over-long residue, and old-shape rejection. Current docs/error text now treat `0x2107` as Read/Write Tag data-type mismatch, not vendor/composite or top-level STRING firmware block. `examples/full_coverage_tags.json` relabels only `ctrl.STRING` and `prog.STRING` as `writeable`; the three full-coverage runners removed the plain-STRING blocked bucket while leaving UDT STRING/member labels restricted. Verification passed: fmt-check, clippy all-targets/all-features, workspace tests with `SKIP_PLC_TESTS=1`, all-target tests, plc_sim_tests 16/16, data_types_showcase example tests, release ffi build, C# unit 86/86, C# native integration 7/7, Python 39 tests/8 skips, validate-agent-files, diff-check, stale-claim grep clean except AT's own relabel instruction. Plain `dotnet test` restore was blocked by an external NuGet scratch lock; `--no-restore` suites passed. Parent-run note: the atomic-shape rejection is demonstrated by direct comparison because parent sim code accepted `0x00CE` STRING writes and returned success; the new test pins the corrected `0xFF/0x2107` behavior. Hardware full-coverage re-run remains the release gate for the manifest relabel (expected post-AT shift: two plain STRING tags write-verified, blocked count reduced by 2).
2026-07-02  claude  [Fable 5]  CODEX-AT  Reviewed and merged at `026c4e2`. Independent verification green: fmt, clippy all-targets all-features, workspace all-targets tests 246/0/48 (data_types_showcase embedded tests now pass), plc_sim_tests 16/16 (three new STRING tests), release ffi build, C# 86/86 + integration 7/7, Python 39/8-skipped, validator. **Review hardware smoke on the 5069-L330ERM fw38 via the fixed library's public API**: reads decode to `PlcValue::String`; single-write round-trips pass controller-scoped AND program-scoped (proving the prog.STRING relabel the original probe had extrapolated); shorter-over-longer residue clean; **batch STRING-only write passes on hardware**; values restored. Instructive false alarm during the smoke: pairing the STRING batch write with a nonexistent tag drew MSP `0x1E` and the library attributed the failure to the whole batch while the controller had applied the valid write — `parse_multiple_service_response` per-service attribution gap recorded in the validation doc as CODEX-AN input; likely origin of the historical "batch STRING fails with 0x1E" lore. Findings: 🟡 strict 90-byte decode requirement (matches all observed hardware); zero defects, zero Claude-applied fixes. Full-coverage hardware re-run before 1.2.0 remains the comprehensive gate.
2026-07-02  claude  [Fable 5]  --  CI red on main triaged (maintainer report) — two pre-existing failures, neither caused by the AK/AJ/AT merges. (1) `cargo audit`: new advisories RUSTSEC-2026-0194/0195 (quick-xml <0.41 NsReader DoS) via desktop_app → eframe 0.27 → winit → sctk → wayland-scanner; targeted `cargo update --precise quick-xml@0.41.0` rejected by the resolver (wayland-scanner 0.31.8 pins lower), so the advisories are ignored with justification (vulnerable parse is build-time-only on local Wayland XML; eframe upgrade tracked in ROADMAP). Discovery: the repo-root `audit.toml` was never read — cargo-audit only honors `.cargo/audit.toml`; the file is now moved there (the four legacy entries are non-fatal warnings, which is why the misplacement went unnoticed). (2) `Test (ubuntu-latest/stable)`: the documented testhost teardown crash recurred on the stable leg (runs 28567033593, 28621972073 — all tests pass, host crashes at exit), eliminating beta-rustc as sole cause; both C# CI steps now retry once and a --blame-crash dump artifact upload (always, 14-day retention) preserves evidence; durable fix routed to CODEX-AS FFI lifecycle scope. `wiki/investigations/dotnet-testhost-shutdown-2026-05-26.md` updated.
2026-07-02  codex  [GPT-5]  CODEX-AM  Submitted: fixed five advertised tag-path addressing behaviors — member-suffixed array writes keep the suffix, `Tag.n` public syntax routes through client-side bit RMW, batch BOOL array operations address the containing DWORD, `.DATA[i]` uses valid Element ID segment encoding, and program tag discovery includes the requested `Program:Name` symbolic scope. Added simulator and pinned-byte coverage for each path, plus simulator parser fixes for padded 16/32-bit element operands. Verification passed: fmt-check, clippy all-targets/all-features, workspace all-features tests with `SKIP_PLC_TESTS=1`, tag-path crate tests, plc_sim_tests 19/19, focused discovery tests, and diff-check. Parent-run note: pre-fix failures were demonstrated by source comparison against `9f5c98b` rather than a separate worktree rerun; the old code paths visibly dropped the suffix, returned/wrote whole words for bit syntax, aliased cross-DWORD batch BOOL elements, emitted malformed `.DATA[i]` bytes, and ignored `_program_name`. Wire changes are flagged for the pre-1.2.0 full-coverage hardware pass.
2026-07-02  claude  [Fable 5]  --  Post-push CI triage round 2 (run 28625781549): audit job green (config-relocation fix proven), ubuntu-stable C# leg green (retry armed). Two failures remained, both CODEX-AT fallout: (1) `tests/full_coverage_manifest_tests.sh` pinned the pre-relabel counts — updated to (2299, 2208 writeable, 72 blocked, 19 read-only), verified locally; (2) structural: the Package-validation job's `cargo package --locked` verifies the tarball against *published* crates.io siblings, so CODEX-AT's new protocol-crate API (`write_data_type_bytes`) cannot compile until rust-ethernet-ip-protocol 1.2.0 ships — the job now runs `--no-verify` mid-cycle with full verified packaging deferred to release-day `check-release-readiness --strict`, per the script's existing design. Any future cross-crate API addition would have re-broken it the same way.
2026-07-03  claude  [Fable 5]  CODEX-AM  Reviewed and merged at `4ec2cec`. Independent verification green: fmt, clippy all-targets all-features, workspace 252/0/48, plc_sim_tests 19/19, tag-path pinned-byte tests, release ffi build, C# 86/86 + integration 7/7, validator. Review hardware smoke on the 5069-L330ERM fw38 exercised all five fixes live (evidence: docs/validation/2026-07-02_tag_addressing_smoke_5069-L330ERM_fw38.md): program discovery returns 7 tags, `.DATA[0]` reads Sint('S'), `Tag.n` bit RMW flipped exactly one bit of a 999999 DINT host, batch BOOL [35] write left [3] untouched, member-suffix write preserved the element. **Headline side-finding: `write_tag("gTestUDT_Array[0].Member1_DINT", Dint)` SUCCEEDED — the "firmware blocks UDT array element member writes" quirk is a second misdiagnosis (0x2107 lore gathered through the malformed paths AM fixed), same class as the STRING finding.** Values restored. Findings: 🟡 Tag.n bit hosts remain DINT/BOOL (typed error for INT/SINT — matches pre-existing envelope, documented gap); 🟢 pre-fix demonstration by source comparison accepted. Zero defects, zero Claude-applied fixes.
2026-07-03  claude  [Fable 5]  CODEX-AV  Brief authored (direct fallout of the AM review finding): systematic maintainer-executed hardware matrix over every `firmware_blocked_*` write class (UDT members per scalar type, STRING members, UDT array element members, program-scoped variants) with restore + sibling-integrity discipline; evidence-based relabel of `examples/full_coverage_tags.json` + runners + pinned counts in one commit; honest rewrite of the quirks note's UDT-array-element section; evidence-gated service-layer decision (direct member write vs RMW fallback). Must land before the pre-1.2.0 full-coverage gate run or the stale blocked-labels will report unexpected-success anomalies. Board release-plan note updated accordingly; pairs naturally with the CODEX-AO phase-2 capture session on the same bench. Status: open.
2026-07-03  codex  [GPT-5]  CODEX-AV  Submitted probe stage: added `examples/probe_blocked_write_labels.rs`, a maintainer-run Rust hardware matrix that selects 11 representative current `firmware_blocked_*` classes by default or all 72 blocked manifest tags with `--all-blocked`; it reads originals, writes type-specific candidates, verifies read-back, checks sibling integrity, restores originals, verifies restore, and emits JSON evidence under `examples/full_coverage_results/`. Added a validation runbook/template at `docs/validation/2026-07-03_blocked_write_label_probe_plan.md`, wired a dry-run assertion into `tests/full_coverage_manifest_tests.sh`, and narrowed current limitation docs so UDT array element member writes are under AV revalidation rather than described as a blanket firmware ban. Verification passed: fmt, probe dry-run 11 targets, probe dry-run --all-blocked 72 targets, clippy on the example, and example test build. Local shell manifest test was not run because WSL/bash is unavailable on this Windows host; CI should cover it. Manifest relabels, pinned count updates, and service-layer routing changes remain gated on the maintainer-run probe evidence.
2026-07-03  claude  [Fable 5]  CODEX-AV  Probe stage reviewed, approved, and merged; **hardware matrix executed during review** (maintainer-authorized bench, 5069-L330ERM fw38). Probe source audited before use — per-target read/write/verify/sibling/restore discipline confirmed. Representative 11 and full 72-target sweeps both RESULT=PASS, unexpected=0: **all 60 scalar UDT-array-element-member targets (DINT/REAL/BOOL/INT, both scopes) write successfully; all 12 STRING-member targets fail 0xFF/0x2107** under the current encoding. Evidence + relabel directive recorded in docs/validation/2026-07-03_blocked_write_label_probe_plan.md; manifest asymmetry found (prog array Member5_String lacks the blocked label its ctrl twin has). Local gates green incl. the full shell suite (Codex's Windows-bash blocker doesn't apply on this host). Task returned to in-progress for the relabel stage: 60 scalar entries → writeable with recomputed pinned counts, STRING members stay blocked with honest label semantics, quirks-note split, service layer routes scalars direct with RMW fallback (STRING members keep RMW). Status: in-progress.
2026-07-03  codex  [GPT-5]  CODEX-AV  Submitted relabel stage: `examples/full_coverage_tags.json` now treats all 60 scalar UDT-array-element-member targets as `writeable`, relabels UDT STRING members to `encoding_blocked_udt_string_member`, and adds the missing program-scope UDT-array-element `Member5_String` entries. Recomputed manifest counts are 2304 total / 2268 writeable / 17 expected-blocked / 19 read-only; Rust/C#/Python full-coverage runners and the shell suite use the same counts and expected-blocked terminology. Service-layer UDT member writes now route non-STRING values direct-first with RMW fallback only on `0x2107`; STRING members keep unconditional RMW. Docs/changelog updated to split scalar-vs-STRING member behavior and to describe STRING members as current-encoding `0x2107` rejections, not firmware bans. Verification passed: fmt, clippy all-targets/all-features, workspace all-features tests with `SKIP_PLC_TESTS=1`, plc_sim_tests, service-layer unit tests, Rust/C#/Python dry-runs, full manifest shell suite under Git Bash wrappers, C# unit tests 86/86, Python 39/8-skipped. Status: submitted.
2026-07-03  claude  [Fable 5]  CODEX-AV  Relabel stage reviewed and merged at `463854a`; task complete. Independent verification green: fmt, clippy all-targets all-features, workspace all-features tests, plc_sim_tests 19/19, shell manifest suite with recomputed pinned counts (2304/2268/17/19) and probe dry-run assertions (4 representative / 17 all-blocked), C# 86/86, Python 31/8-skipped, validator 48 files. Manifest label census and count arithmetic verified independently (72 − 60 relabeled + 5 added prog Member5_String = 17 blocked). Service layer routes non-STRING member writes direct-first with whole-UDT RMW fallback gated on the 0x2107 shape, STRING members RMW-only, with three routing unit tests via a private strategy trait. Doc sweep verified by grep — disproved claims survive only under explicit historical banners. One Claude-applied fix: evidence doc now states the five added prog-scope Member5_String entries were unprobed/class-inferred (gate run exercises them directly). Findings: 🟡 direct-first covers non-probed value kinds (Udt, SINT/LINT) mitigated by the 0x2107 fallback; 🟡 0x2107 detection string-matches formatter text (typed accessor belongs to error consolidation); 🟡 single-controller evidence scope recorded everywhere. Zero defects. The pre-1.2.0 full-coverage gate is un-broken; remaining open set: AL, AN, AO, AP, AQ, AR, AS, AU.
2026-07-06  codex  [GPT-5]  CODEX-AN  Submitted: `read_array_range` chunk parsing now uses `CipResponse` and reads Logix Read Tag payloads as `[type u16][data...]` with no synthetic element-count word; BOOL-array DWORD parsing uses the same response codec. `get_tag_attributes` now builds service `0x03` with `CipRequest` path-size/padding and parses Get Attribute List records by attribute id/status/value. The simulator no longer injects multi-element element counts and now serves Symbol Get Attribute List records for known tags. Added sim tests for DINT/REAL range reads across the chunk boundary and attribute known/unknown paths, plus pinned request/response unit tests. Reconciled hardware evidence: full-coverage runners use per-element reads, not this public range API. Verification passed: fmt, old-heuristic grep, clippy all-targets/all-features, locked workspace tests with `SKIP_PLC_TESTS=1`, `plc_sim_tests` 21/21, `discovery_tests` 9/9, C# build, C# unit 86/86, C# integration 7/7, and `validate-agent-files` 48 files. Hardware smoke before 1.2.0 must capture DINT+REAL `read_array_range` over a chunk boundary and one known-tag `get_tag_attributes`.
2026-07-06  claude  [Opus 4.8]  CODEX-AN  Reviewed and merged at `8645f67`. Independent full-matrix verification green: fmt, old-heuristic grep clean, clippy all-targets/all-features `-D warnings`, `SKIP_PLC_TESTS=1 cargo test --workspace --locked`, plc_sim_tests 21/21 (four new AN tests), discovery unit 9/9 (two new pinned request/response tests), release ffi build, C# unit 86/86, C# integration 7/7. Root cause confirmed at byte level: the old array parse landed on offset 8 (`4 header + 2 type + 2 phantom element-count`) so removing the sim's phantom word exposes the two-byte data drop; the old attribute request omitted the mandatory request-path-size word and the parser read into the CIP reply header + attribute-record framing. Both paths rebuilt on the `CipRequest`/`CipResponse` codec (`cip-framing.md` boundary), with `status != 0` checked before `.data` so additional-status error replies are handled correctly. The DINT/REAL range tests now cross the 8-element chunk boundary and pin element[8]/[9] — the exact bytes the old parse corrupted. Step-zero reconciliation was performed and recorded (full-coverage runners expand ranges into per-element `Array[i]` reads, never the chunked parser — no contradiction with the 2299/2299 hardware pass). Findings: 🟡 `TagAttributes.size` is now a derived scalar element width surfaced via FFI `TagAttributesC.size` (no internal consumer; prior wire value was garbage — an improvement); 🟢 stale "at least 6/8" error strings next to changed guards (cosmetic); 🟢 old-code-fails demonstration by byte-analysis rather than a live failing run, and the struct-read `symbol_id` fallback sim test deferred to CODEX-AO (sim doesn't model struct reads) — both accepted by AM/AT precedent. Zero defects, zero Claude-applied fixes. Mandatory pre-1.2.0 maintainer hardware smoke recorded (DINT+REAL `read_array_range` across a chunk boundary + one `get_tag_attributes`, with packet capture). Remaining open set: AL, AO, AP, AQ, AR, AS, AU.
2026-07-06  codex  [GPT-5]  CODEX-AL  Submitted: transport/session hardening implemented. `session_handle` is now Arc-shared across clones; `register_session` uses one stream lock and exact header/body framing; SendRRData marks the stream poisoned for every in-flight transaction, clears it only after a complete response, stamps a monotonic sender_context, and rejects mismatched contexts; keep-alive respects poison. The test and standalone simulators now echo sender_context, with test-only corruption injection. FFI `store_client` and health/diagnostics reinserts are gone, so `eip_disconnect` cannot be undone by a cloned detailed-health call. Verification passed: `cargo fmt -- --check`, clippy all-targets/all-features, locked workspace tests with `SKIP_PLC_TESTS=1`, `plc_sim_tests` 22/22, `transport_tests` 2/2, FFI state consistency 3/3, release FFI build, C# build, C# unit 86/86, and C# integration 7/7. Hardware smoke before 1.2.0 should capture normal read/write SendRRData and confirm controllers echo non-constant sender_context values.
2026-07-06  claude  [Opus 4.8]  CODEX-AL  Reviewed and merged at `253706e`. Independent full-matrix verification green: fmt, `store_client` grep clean, clippy all-targets/all-features `-D warnings`, `SKIP_PLC_TESTS=1 cargo test --workspace --locked`, transport_tests 2/2, ffi_state_consistency 3/3 (incl. disconnect-vs-detailed-health resurrection regression), plc_sim_tests 22/22 (timeout-poison + sender_context-mismatch), release ffi build, C# unit 86/86, C# integration 7/7. All four §2/§3 transport findings correctly fixed: `session_handle` → `Arc<AtomicU32>` restores one-handle-per-stream AND makes the FFI `store_client` re-insert redundant (the re-insert existed only because the clone-copied `u32` didn't propagate — removing it closes the disconnect-resurrection race); `stream_poisoned: Arc<AtomicBool>` set-before-write/clear-after-complete-read is cancellation-safe (dropped future, write error, timeout, or context mismatch all leave it poisoned → next call fast-fails `ConnectionLost`); monotonic `sender_context` (counter from 1) stamped via new `send_rr_data_with_context` and verified on receive; `register_session` single-locked with `read_exact` header+body framing. Error-status branch drains the body then clears poison (stream clean). Clone-contract comments accurately rewritten; `Send+Sync+'static` assertion holds. Findings: 🟡 the `sender_context` mismatch check runs before the status check and applies to encapsulation-error (`status != 0`) replies too — happy path and CIP errors are spec-safe (encap layer echoes context), narrow risk only on genuine encap-layer errors from a non-echoing controller; brief-anticipated, gated on the hardware smoke, ~2-line relaxation if needed (#1 smoke decision point). 🟢 `EncapsulationHeader::send_rr_data` (constant-context builder) now has zero `src/` call sites — dead weight, `pub`, deferred to the 2.0 API sweep. Zero defects, zero Claude-applied fixes. Mandatory pre-1.2.0 maintainer hardware smoke recorded (normal read/write capture confirming per-request `sender_context` echo). Remaining open set: AO, AP, AQ, AR, AS, AU.
2026-07-06  codex  [GPT-5]  CODEX-AS  Submitted: FFI polish implemented after CODEX-AL. Public raw-pointer exports `eip_get_udt_definition`, `eip_get_tag_attributes`, and `eip_discover_tags_detailed` are now private helpers behind the `_by_id` ABI, so `ABI_VERSION` is bumped to 2 and C#/Python pins are updated. `ffi_block_on!` catches panics and records `internal panic: ...` in last-error; poisoned FFI locks recover with warnings; client-id wrap scans for an unoccupied id; disconnect removes the client, best-effort unregisters the session, and clears last-error. Last-error paths now clear on success and set messages for audited `-1` returns across scalar/string/UDT/batch operations; `eip_read_string` no longer scans UDT bytes as ASCII; `eip_write_udt` no longer sends empty fallback payloads. Python now has weakref finalization, disconnect state clearing on native failure, and per-candidate ABI mismatch handling during library search. Verification passed: fmt, clippy all-targets/all-features, explicit undocumented-unsafe-block lint, locked workspace tests with `SKIP_PLC_TESTS=1`, ffi_abi 2/2, ffi_tests 15/15, ffi_state_consistency 3/3, plc_sim_tests 22/22, release FFI build with objdump raw-symbol check, C# build, C# unit 86/86, C# integration 7/7, and Python 42/8-skipped.
2026-07-06  claude  [Opus 4.8]  CODEX-AS  Reviewed and merged at `00db89e`. Independent full-matrix verification green: fmt, clippy all-targets/all-features `-D warnings`, `undocumented_unsafe_blocks` lint clean (every unsafe block has a `// SAFETY:`), `SKIP_PLC_TESTS=1 cargo test --workspace --locked`, ffi_abi 2/2, ffi_tests 15/15, ffi_state_consistency 3/3, plc_sim_tests 22/22, release ffi build. **objdump on the actual release DLL independently confirms the symbol removal** — the three raw `*mut EipClient` exports are absent, the three `_by_id` variants present, 59 `eip_*` exports total. C# 86/86 + integration 7/7; Python 42/8-skipped (all four Python/C# items verified via subagent, ABI pins consistent across all five locations). All seven §3 findings + Python residuals land: `catch_unwind` in the shared `ffi_block_on!` macro contains host-aborting panics (→ `-1` + `internal panic:` last-error); poisoned locks recover via `into_inner`+warning; `allocate_client_id` occupancy-scans and reports exhaustion; `eip_disconnect` removes-first then best-effort unregisters under its own catch_unwind then drops last-error; `eip_read_string` strict String-only, `eip_write_udt` no empty fallback (both with descriptive last-error, pinned tests); UDT allocator avoids `malloc(0)` and unwind-frees + zeroes out-params on every error path; last-error clears on success. Findings: 🟡 **maintainer release decision (not a defect)** — ABI v2 is a linker-visible removal of unusable symbols; defensible as 1.2.0-minor (removed symbols unusable by construction; wrappers import only `_by_id` and move in lockstep; crates.io SemVer untouched; `eip_abi_version()` fails-fast direct C consumers), but wants tag-time sign-off. Board 1.2.0 note footnoted accordingly. 🟢 catch_unwind covers the async runtime future not the thin sync marshalling (honestly documented); 🟢 Python disconnect clears state before the native call so a native failure can leak the handle (brief-mandated tradeoff); 🟢 a few SAFETY comments lean generic, `remove_last_error` aliases `clear_last_error` (cosmetic). Zero defects, zero Claude-applied fixes. Unblocks CODEX-AU (C header excludes the removed raw trio) and carries the durable fix for the dotnet testhost-shutdown crash. Remaining open set: AO, AP, AQ, AR, AU.
2026-07-06  claude  [Opus 4.8]  --  Post-push CI triage (main red after the AN/AL/AS push). Four failures, three fixed on main. (1) **semver-checks — CODEX-AS fallout, owned review error:** the AS review wrongly claimed crate SemVer was untouched; `cargo-semver-checks` flagged the three removed `pub unsafe extern "C" fn` FFI exports as `function_missing` (major). Per maintainer direction (keep the C-ABI removal, stay 1.2.0-minor), the three functions were restored as non-`#[no_mangle]` `pub unsafe extern "C" fn`s delegating to their `_impl` helpers (objdump reconfirms the C symbol table still lacks them — 59 exports, `_by_id` only). Restoring surfaced `function_export_name_changed` (semver-checks also tracks the `#[no_mangle]` ABI name); resolved with a narrow `[package.metadata.cargo-semver-checks.lints] function_export_name_changed = "allow"` — FFI-symbol-only, so `function_missing` still guards ordinary Rust API. `check-release --baseline-version 1.1.0` now passes (222 checks). ABI stays v2; C#/Python pins unchanged; `ffi_abi.rs` export test reworked to assert no-`#[no_mangle]` (CRLF-normalized). (2) **clippy — toolchain drift, not ours:** a newer CI clippy's `chunks_exact_to_as_chunks` fired on untouched `crates/udt/src/lib.rs:282`; rewrote `chunks_exact(8)` → `as_chunks::<8>().0.iter()` (MSRV 1.96 ≥ 1.88, behavior-identical, udt 5/5). (3) **cargo audit — advisory drift, not ours:** RUSTSEC-2026-0204 (`crossbeam-epoch` 0.9.18, real vuln via the `criterion` dev-dep) fixed by a precise bump to 0.9.20; `ttf-parser`/`memmap2` (desktop_app example) + `anyhow` (build-dep via `vergen`) added to `.cargo/audit.toml` ignore with provenance. Re-verified green: semver-checks, clippy all-targets/all-features `-D warnings`, `undocumented_unsafe_blocks`, ffi_abi 2/2 + ffi_tests 15/15 + ffi_state_consistency 3/3, plc_sim_tests 22/22, workspace `--locked`, release ffi build + objdump, C# 86/86. Docs updated (CHANGELOG, abi-contract.md, ffi-safety.md) to record that the C ABI is versioned by `ABI_VERSION`, not crate SemVer.
2026-07-07  codex  [GPT-5]  CODEX-AP  Submitted: retired the string/UDT strategy graveyard with honest compatibility errors. Legacy STRING APIs (`write_string`, `write_ab_string_components`, `write_ab_string_udt`, `write_string_connected`, `write_string_unconnected`) now return `EtherNetIpError::Unsupported` immediately and are deprecated for 2.0 removal; the connected messaging subsystem and orphaned connected-session/raw-helper internals were deleted. `read_udt_chunked` now delegates to the maintained `read_tag` UDT path and no longer dispatches on `"Partial transfer"` text or fabricates empty UDTs; offset-based UDT member Rust/FFI/C# APIs return explicit unsupported errors and C# wrappers are `[Obsolete]`. Batch helper `unreachable!`s became typed internal `BatchError::Other` results. Extended-status parsing now keys on additional-status size and decodes little-endian status words only, with `0x2107` reported as Read/Write Tag data-type mismatch. Docs/wiki updated. Verification passed: fmt, clippy all-targets/all-features, workspace all-features tests with `SKIP_PLC_TESTS=1`, plc_sim_tests 24/24, ffi_tests 16/16, docs, semver-checks against 1.1.0, release FFI build, Python 42/8-skipped, C# unit 86/86, C# native integration 7/7, and `git diff --check`.
2026-07-07  claude  [Opus 4.8]  CODEX-AP  Reviewed and merged at `1821e59`. Independent full-matrix verification green: fmt, clippy all-targets/all-features `-D warnings`, `SKIP_PLC_TESTS=1 cargo test --workspace --all-features --locked`, plc_sim_tests 24/24 (two new AP tests), ffi_tests 16/16, semver-checks baseline 1.1.0 (221 pass, no update required), `cargo doc --workspace --all-features --no-deps` clean (no broken intra-doc links), release ffi build, C# unit 86/86, C# native integration 7/7 against the freshly built cdylib, Python 34-pass/8-skip. All eight brief items dispositioned: `src/client/string.rs` drops 1041→89 lines; the five retired writers return `EtherNetIpError::Unsupported` before any I/O; the connected-messaging subsystem + `ConnectedSession`/`ConnectionParameters` (`pub(crate)`) deleted; `read_udt_chunked` delegates to `read_tag` (no `"Partial transfer"` string dispatch — grep clean); offset UDT APIs (Rust+FFI+C#) deprecated/`[Obsolete]`+honest error; two batch `unreachable!` → typed `BatchError::Other`; `parse_extended_error` rekeyed on additional-status size, LE-only, `0x2107` mapped, `check_cip_error` gate corrected to `cip_data[3] > 0`. Key judgment call verified correct: FFI `eip_write_string` repointed to the working `write_tag(PlcValue::String)` path rather than retired, so the C ABI / C#/Python `WriteString` contract is unchanged (no consumer regression). `Unsupported` variant is additive on the `#[non_exhaustive]` enum; new `type_method_marked_deprecated = "allow"` semver lint is minor-compatible (removals still guarded by `function_missing`). Findings: 🟡 `read_udt_chunked` on an oversized (>packet) UDT now returns the propagated `read_tag` error instead of the old fabricated `Ok(Udt{data:vec![]})` — strictly an improvement, real fragmented-read support owned by CODEX-AO phase 2; 🟡 `check_cip_error` now routes to extended parsing on any non-zero additional-status size (was `0xFF`-gated), spec-correct and affects only the error string; 🟢 the retired-string sim test asserts the typed error rather than absence of wire bytes (stubs demonstrably return pre-I/O). Zero defects, zero Claude-applied fixes. Remaining open set: AO, AQ, AR, AU.
2026-07-07  claude  [Opus 4.8]  CODEX-AQ  Brief refreshed against `main` at `7cc17ac` (the 2026-07-01 Fable-5 brief predated the AL/AP merges). File:line references re-verified and corrected post-AP line drift (`discover_udt_members` now `client.rs:530`; real template path `get_udt_definition` now `client.rs:1850`; `build_udt_definition_request` now `tag_manager.rs:345`); item 1 rewritten to name the actual inline `build_udt_definition_request`/`parse_udt_definition_response` pair rather than the private `TagManager::get_udt_definition`, with a "verify `self.tag_manager` has no other live consumer before deprecating the type" guard. Item 4 diagnostics-honesty resolved the CODEX-AL coordination note (AL merged `253706e`; counters land on the settled post-AL `send_rr_data_item`). Added a semver-checks gotcha: this task deprecates whole `pub` structs/modules so it will trip the struct/enum/type-level `*_marked_deprecated` lints — add narrow `Cargo.toml` allows following the AP precedent, stop-and-report on any MAJOR flag. Context section now points at CODEX-AP (`1821e59`) as the immediate precedent (shared `Unsupported` variant, `#[expect(deprecated)]` pattern, lint-allow precedent). Status stays open; next up per the recommended sequence (AS → AP → **AQ** → AR → AU).
2026-07-07  codex  [GPT-5]  CODEX-AQ  Submitted: dead-stratum deprecation and diagnostics honesty pass implemented. `EipClient::discover_udt_members` and TagManager member discovery now use the real template-definition path; the old `build_udt_definition_request` / `parse_udt_definition_response` behavior is gone, with public names retained only as deprecated `Unsupported` stubs for 1.x SemVer. Live discovery accepts legal Logix names, recognizes `0x02A0` structure words, errors on malformed tag-list pages instead of byte-pattern resync, and stops inventing zero array dimensions. Deprecated `ProductionMonitor`, `ProductionConfig`, `SubscriptionManager`/`RealTimeSubscriptionManager`, `TagCache`, and `PlcManager` with replacement notes and internal `#[expect(deprecated)]` hygiene; `ProductionMonitor::start_monitoring` is a no-op warning instead of a leaking task, and `MonitoringMetrics::system_metrics_are_placeholders()` exposes the legacy system-metric placeholder status without adding a public field. Diagnostics snapshots now report per-client atomic read/write/batch/error counters from the CIP send path. PlcManager tests migrated to direct/mock clients; fake `udt_discovery_benchmark` deleted; Tokio `signal` enabled so the simulator example clean-builds for C# tests; Cargo semver lint metadata, CHANGELOG, ROADMAP, task board, and wiki updated. Verification passed: fmt-check, clippy workspace all-targets/all-features, `SKIP_PLC_TESTS=1 cargo test --workspace --all-features --locked`, `cargo test --test plc_sim_tests`, bench compile, release FFI build plus `eip_connect` export spot check, docs, semver-checks vs 1.1.0 (220 pass), Python 34/8 skipped, C# unit 86/86, C# native integration 7/7, validator, diff-check, and hygiene grep.
2026-07-07  claude  [Opus 4.8]  CODEX-AQ  Reviewed and merged at `272e0ae`. Independent full-matrix verification green: fmt, clippy all-targets/all-features `-D warnings`, `SKIP_PLC_TESTS=1 cargo test --workspace --all-features --locked` (all suites 0 failed, incl. new tag_manager discovery-filter tests), plc_sim 24/24, semver-checks baseline 1.1.0 (220 pass, no update), `cargo bench --no-run` (udt_discovery gone), `cargo doc` clean, release ffi build, C# unit 86/86, C# native integration 7/7 against the freshly built cdylib, Python 34-pass/8-skip. All six items dispositioned: `discover_udt_members` (`client.rs:814`) delegates to the real `get_udt_definition` template path — no more invented `Member_N`/`Value:DINT`; the fabricating `build_udt_definition_request`/`parse_udt_definition_response` kept as deprecated `Unsupported` stubs (SemVer-correct vs the brief's "delete", since they were `pub`), old byte-pinned tests gone (grep clean). Discovery filters fixed: `TAG_NAME_RE`/`validate_tag_name` accept `_Tag`/`Program:Main.Tag`/`Arr[3]`; `is_structure_type_word` keys on bit-15/`0x02A0`; `parse_tag_list` is count-driven off a correctly-offset `item_count` with strict truncation errors (four-zero-byte resync + fictional `vec![0;dims]` gone); `RwLock` unwraps → `?`/`.ok()` (only test unwraps remain). Dead structs deprecated (`ProductionMonitor`, `ProductionConfig`, `SubscriptionManager`/alias, `TagCache`, `PlcManager`) with 2.0-queued removals; `start_monitoring` no longer leaks a task; `system_metrics_are_placeholders()` honesty flag. `DiagnosticCounters` (`client.rs:65`, `Arc`-shared on clone) are real atomics wired at the `send_cip_request` chokepoint (classified by CIP service byte) + batch; snapshot reads from them, hardcoded zeros gone. `ffi.rs` untouched → diagnostics JSON schema unchanged (no wrapper-parse risk). `Cargo.toml` `type_marked_deprecated = "allow"` mirrors the AP lint precedent (minor-compatible; `function_missing` still guards removals); `tokio` `signal` add is genuine (`python_test_simulator.rs` `ctrl_c`). Findings: 🟡 `validate_tag_name` still drops double-underscore names (`My__Tag`) via `contains("__")` — legal in Logix, rare, follow-up; 🟡 `send_cip_request` records success when `extract_unconnected_data_item` fails on a malformed CPF (`:3521`) — slight success over-count in a rare path, cosmetic; 🟢 `subscription_updates` stays 0 (honest "not tracked"). Zero defects, zero Claude-applied fixes. Residual: sim/unit-level only — real multi-page hardware tag-list discovery + counter accumulation fold into the standard pre-1.2.0 hardware smoke. Remaining open set: AO, AR, AU.
